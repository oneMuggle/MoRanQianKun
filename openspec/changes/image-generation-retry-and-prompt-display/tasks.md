# 实现任务清单

## 1. 配置结构扩展

- [x] 1.1 在 `utils/apiConfig.ts` 中为 `功能模型占位配置结构` 添加 `提示词生成重试次数` 和 `图片生成重试次数` 字段，添加对应的规范化逻辑
  - **完成标准**: 类型定义完整，默认值为 1，范围验证 0-5

- [x] 1.2 在 `models/imageGeneration.ts` 中扩展 `NPC生图结果记录` 类型，确认 `保留提示词` 字段存在（如需要）
  - **完成标准**: 现有类型已包含 `生图词组`、`最终正向提示词`、`最终负向提示词`，无需额外字段

## 2. NPC 生图工作流改造

- [x] 2.1 修改 `hooks/useGame/npcImageWorkflow.ts`，确保失败时提示词数据已保存
  - **完成标准**: catch 块中正确保存提示词到最近生图结果

- [x] 2.2 为 `执行NPC生图工作流` 添加 `重试选项` 参数支持（完全重试/复用提示词）
  - **完成标准**: 函数签名包含 `重试选项` 和 `重试次数` 参数

- [x] 2.3 实现「复用提示词重试」逻辑：跳过提示词生成阶段，直接使用已有提示词调用图片生成
  - **完成标准**: 复用提示词时正确调用 `generateImageByPrompt`

## 3. 场景生图工作流改造

- [ ] 3.1 修改 `hooks/useGame/sceneImageWorkflow.ts`，确保失败时提示词数据已保存
  - **完成标准**: catch 块中正确保存提示词到最近生图结果

- [ ] 3.2 为 `执行场景生图工作流` 添加 `重试选项` 参数支持
  - **完成标准**: 函数签名包含 `重试选项` 和 `重试次数` 参数

- [ ] 3.3 实现场景图片的「复用提示词重试」逻辑
  - **完成标准**: 复用提示词时正确调用图片生成服务

## 4. NPC 秘档部位生图工作流改造

- [ ] 4.1 修改 `hooks/useGame/npcSecretImageWorkflow.ts`，确保失败时提示词数据已保存
  - **完成标准**: catch 块中正确保存提示词到最近生图结果

- [ ] 4.2 为 NPC 秘档部位生图添加 `重试选项` 参数支持
  - **完成标准**: 函数签名支持两种重试模式

## 5. useGame 层对接

- [x] 5.1 在 `hooks/useGame.ts` 中查找现有的图片重试处理逻辑
  - **完成标准**: 了解现有 `handleRetryImage` 或相关 action 的实现

- [x] 5.2 扩展现有重试逻辑，支持两种重试模式选择
  - **完成标准**: action 接受 `模式` 参数（完全重试/复用提示词）

- [x] 5.3 在 useGame 导出的 actions 中添加场景图片重试处理
  - **完成标准**: 存在可调用的 `handleRetrySceneImage` 或类似方法

## 6. 设置页面扩展

- [x] 6.1 在 `components/features/Settings/ImageGenerationSettings.tsx` 中添加「重试设置」页签
  - **完成标准**: 页面导航包含新的页签选项

- [x] 6.2 实现重试次数配置 UI（数字输入框，范围验证 0-5）
  - **完成标准**: 输入框显示默认值，支持保存到配置

- [x] 6.3 连接配置保存逻辑到 `onSave`
  - **完成标准**: 保存后配置正确持久化

## 7. 图片管理界面增强

- [ ] 7.1 在 `components/features/Social/ImageManagerModal.tsx` 中为失败记录添加「查看提示词」按钮
  - **完成标准**: 失败记录显示操作按钮

- [ ] 7.2 创建提示词展示弹窗组件
  - **完成标准**: 弹窗显示生图词组、正向提示词、负向提示词、错误信息

- [ ] 7.3 为失败记录添加「完全重试」和「复用提示词重试」按钮
  - **完成标准**: 两个按钮正确显示，复用按钮在无提示词时禁用

- [ ] 7.4 实现重试按钮的点击处理，调用 useGame 中的重试 action
  - **完成标准**: 点击按钮后正确触发重试流程

- [ ] 7.5 添加复制提示词到剪贴板功能
  - **完成标准**: 复制按钮工作，UI 显示反馈

## 8. 测试与验证

- [x] 8.1 本地运行 `npm run build` 确保无编译错误
  - **完成标准**: 构建成功，无类型错误

- [ ] 8.2 测试配置保存和加载
  - **完成标准**: 重试设置正确保存和读取

- [ ] 8.3 测试完整重试流程
  - **完成标准**: 完全重试生成新的提示词和图片

- [ ] 8.4 测试复用提示词重试流程
  - **完成标准**: 复用提示词时使用原有提示词生成新图片

- [ ] 8.5 测试提示词展示功能
  - **完成标准**: 弹窗正确显示所有提示词内容

## 9. 移动端适配（如需要）

- [x] 9.1 检查移动端图片管理界面是否需要相同改动
  - **完成标准**: 评估是否需要移动端适配（设计已明确不涉及移动端改动）

- [x] 9.2 如需要，修改 `components/features/Social/mobile/MobileImageManagerModal.tsx`
  - **完成标准**: 移动端界面功能完整（本次无需改动）