/**
 * 都市网约车 NSFW 运行时提示词模块
 *
 * 为 AI 叙事生成提供行程场景判定、乘客欲望描述、后果叙事等 prompt 组件
 */

import type {
  乘客欲望阶段,
  行程关系轨道,
  醉酒状态,
  药物状态,
} from '../../models/urbanDriverNSFW/core';
import type {
  行程NSFW类型,
} from '../../models/urbanDriverNSFW/scenarios';
import type {
  网约车后果类型,
} from '../../models/urbanDriverNSFW/consequences';

// ==================== 行程 NSFW 叙事约束 ====================

const 行程NSFW叙事描述: Record<行程NSFW类型, string> = {
  '醉酒搭车': '深夜接到醉酒女乘客，在密闭车厢内酒精催化下产生暧昧互动。重点描写醉态下的反差：白天理性克制的人在车厢中卸下防备，眼神迷离、语言含糊、身体不受控制地靠近。突出"明天醒来会怎样"的焦虑与当下放纵的矛盾。',
  '饮料下药': '车内饮料被下药导致意识逐渐模糊。重点描写意识与身体的分离感：大脑越来越迟钝但身体的触感变得格外清晰。强调判断力下降带来的危险与刺激并存。',
  '深夜独处': '凌晨空旷街道上只有司机和单一乘客。重点描写深夜孤独感催化的暧昧：城市的灯光透过车窗在两人脸上流转，发动机的低鸣是唯一的背景音。突出"移动的密室"氛围。',
  '后座暗示': '乘客在后座做出暗示性行为或语言。重点描写后视镜中的目光交错、欲言又止的对话、肢体语言的微妙变化。突出"不知道对方是否察觉"的紧张感。',
  '停车场秘密': '到达目的地后谁都没有先下车，在停车场延伸的故事。重点描写引擎熄火后的沉默、车外的空旷与车内的暧昧对比、"再等一会儿"的借口。',
  '拼车暧昧': '多名乘客同乘时的复杂互动。重点描写乘客之间的微妙关系、眼神交流中的竞争与合作、在有限空间内的身体接触。突出多人动态的复杂性。',
  '常客关系': '固定乘客在日复一日的相处中逐渐产生的变化。重点描写从礼貌客气到暧昧熟悉的渐进、每天细微的观察积累、"今天ta又换了新香水"的在意。',
  '行车记录仪': '知道正在被录制的紧张感催化下的互动。重点描写记录仪红灯的心理压迫、"这个角度会被拍到"的自觉、每一个动作都被留存的紧张与刺激。',
};

/**
 * 构建行程 NSFW 叙事约束
 */
export const 构建行程NSFW叙事约束 = (
  行程类型: 行程NSFW类型,
  内容强度: '微暗' | '暧昧' | '露骨'
): string => {
  const 强度规则: Record<string, string> = {
    '微暗': '侧重心理描写和氛围营造，用含蓄的语言暗示车厢内的暧昧感',
    '暧昧': '适度描写情感互动和身体感受，保留都市特有的青涩感',
    '露骨': '直接描写不回避，但保持叙事质量和角色性格的一致性',
  };

  return `【${行程类型}叙事约束】
${行程NSFW叙事描述[行程类型]}
描写强度: ${强度规则[内容强度]}`;
};

// ==================== 醉酒叙事约束 ====================

const 醉酒等级叙事: Record<NonNullable<醉酒状态>['等级'], string> = {
  '清醒': '乘客意识清晰，行为理性，互动正常',
  '微醺': '乘客面颊微红，说话偶尔停顿，目光比平时更大胆，身体偶尔不自觉地靠近',
  '醉酒': '乘客眼神迷离，语言含糊不清，动作不受控制，平时不会做的事在酒精催化下变得"合理化"',
  '烂醉': '乘客几乎失去自理能力，身体软绵绵地靠在座位上，偶尔呢喃，意识断断续续',
};

/**
 * 构建醉酒叙事约束
 */
export const 构建醉酒叙事约束 = (
  醉酒状态: 醉酒状态,
  内容强度: '微暗' | '暧昧' | '露骨'
): string => {
  const 行为描述 = 醉酒等级叙事[醉酒状态.等级];

  return `【醉酒叙事约束】
当前状态: ${醉酒状态.等级} (行为大胆度: ${醉酒状态.行为大胆度}%, 记忆模糊度: ${醉酒状态.记忆模糊度}%)
${行为描述}
${醉酒状态.判断力下降 ? '判断力已下降，平时不会做的行为在此时可能被合理化' : ''}
描写强度: ${内容强度 === '露骨' ? '直接描写醉态下的身体反应和行为失控' : '侧重描写醉酒后的心理变化和氛围'}`;
};

// ==================== 下药叙事约束 ====================

const 药物生效叙事: Record<NonNullable<药物状态>['生效阶段'], string> = {
  '未生效': '药物尚未生效，乘客意识正常',
  '初期': '药物开始生效，乘客感到轻微的头晕和乏力，以为是疲劳所致',
  '强烈': '药物强烈生效，乘客意识模糊，身体不听使唤，大脑能思考但无法控制行动',
  '衰退': '药物效果逐渐消退，乘客意识缓慢恢复，感到困惑和不安',
};

/**
 * 构建下药叙事约束
 */
export const 构建下药叙事约束 = (
  药物状态: 药物状态,
  内容强度: '微暗' | '暧昧' | '露骨'
): string => {
  const 生效描述 = 药物生效叙事[药物状态.生效阶段];
  const 药物类型叙事: Record<NonNullable<药物状态>['类型'], string> = {
    '迷药': '迷药导致意识逐渐模糊，身体比意识更先做出反应',
    '安眠药': '安眠药导致强烈的睡意，身体越来越无力，眼皮沉重',
    '兴奋剂': '兴奋剂导致异常的亢奋，心跳加速，感官变得格外敏锐',
    '未知': '不明药物导致异常的身体反应，意识与感知出现错位',
  };

  return `【下药叙事约束】
药物类型: ${药物类型叙事[药物状态.类型]}
生效阶段: ${药物状态.生效阶段} (意识清晰度: ${药物状态.意识清晰度}%, 身体控制度: ${药物状态.身体控制度}%)
${生效描述}
描写强度: ${内容强度 === '露骨' ? '直接描写药物影响下的身心分离' : '侧重描写药物带来的异常感受和意识模糊'}`;
};

// ==================== 行车记录仪紧张度约束 ====================

/**
 * 构建行车记录仪紧张度约束
 */
export const 构建行车记录仪紧张度约束 = (
  紧张度: number,
  记录仪状态: '关闭' | '录制中' | '已泄露'
): string => {
  if (记录仪状态 === '关闭') return '';

  const 紧张度描述 = 紧张度 >= 80 ? '极度紧张：每一个动作都像在聚光灯下，记录仪的红光如同悬在头顶的剑'
    : 紧张度 >= 50 ? '中度紧张：时常瞟向记录仪，担心正在录制的画面会被留存'
    : '轻微紧张：隐约意识到记录仪的存在，但尚未过度关注';

  return `【行车记录仪紧张度约束】
记录仪状态: ${记录仪状态}
当前紧张度: ${紧张度}%
${紧张度描述}
${记录仪状态 === '录制中' ? '记录仪正在录制，车厢内的一切都被留存。善用红灯闪烁、角度变化等细节营造紧张感' : ''}
${记录仪状态 === '已泄露' ? '记录仪画面已被泄露，这个事实本身就是一个巨大的叙事压力' : ''}`;
};

// ==================== 后果叙事约束 ====================

const 后果叙事: Record<网约车后果类型, string> = {
  '平台投诉': '乘客或他人向平台投诉司机行为不当，平台发来警告通知',
  '行车记录仪曝光': '行车记录仪中的画面被泄露，开始在特定范围内传播',
  '药物后遗症': '药物导致的健康问题开始显现，需要处理',
  '差评降权': '平台评分下降，接单优先级降低，收入受到影响',
  '乘客依赖': '乘客对司机形成了心理依赖，行为开始出现异常',
  '网络传播': '事件在社交媒体上发酵，截图或录音被广泛转发',
  '警察盘查': '路遇临检，异常情况引起警察注意',
  '同行竞争': '其他司机发现异常，嫉妒或威胁的行为开始出现',
  '常客流失': '关系破裂导致失去固定客源',
  '封号处罚': '平台正式封禁账号，职业生涯面临重大打击',
  '勒索威胁': '第三方掌握证据并以此勒索',
  '情感纠葛': '多名乘客之间的关系出现冲突',
};

/**
 * 构建网约车后果叙事约束
 */
export const 构建网约车后果叙事约束 = (
  后果类型: 网约车后果类型,
  严重程度: '轻微' | '中等' | '严重' | '毁灭',
  NPC信息?: string
): string => {
  const 严重度修饰: Record<string, string> = {
    '轻微': '轻微的影响，暂时不会造成严重后果',
    '中等': '中等程度的影响，需要关注和处理',
    '严重': '严重的影响，可能对职业生涯造成实质性打击',
    '毁灭': '毁灭性的后果，可能彻底改变人生轨迹',
  };

  return `【后果叙事】
后果类型: ${后果类型}
严重程度: ${严重程度} - ${严重度修饰[严重程度]}
${后果叙事[后果类型]}
${NPC信息 ? `涉及NPC: ${NPC信息}` : ''}`;
};

// ==================== 完整叙事约束 ====================

/**
 * 构建都市网约车完整叙事约束
 * 综合所有子系统状态生成统一的 prompt 注入
 */
export const 构建都市网约车完整叙事约束 = (参数: {
  行程类型?: 行程NSFW类型;
  乘客欲望阶段?: 乘客欲望阶段;
  关系轨道?: 行程关系轨道;
  暴露风险?: number;
  紧张度?: number;
  醉酒状态?: 醉酒状态;
  药物状态?: 药物状态;
  行车记录仪状态?: '关闭' | '录制中' | '已泄露';
  内容强度?: '微暗' | '暧昧' | '露骨';
  后果?: { 类型: 网约车后果类型; 严重程度: '轻微' | '中等' | '严重' | '毁灭'; NPC信息?: string };
}): string => {
  const {
    行程类型,
    乘客欲望阶段,
    关系轨道,
    暴露风险,
    紧张度,
    醉酒状态,
    药物状态,
    行车记录仪状态,
    内容强度,
    后果,
  } = 参数;

  const 强度 = 内容强度 ?? '暧昧';
  const 组件: string[] = [];

  // 网约车系统激活标记：即使行程尚未开始，也要在叙事中埋下氛围种子
  if (!行程类型 && 乘客欲望阶段 === '克制') {
    组件.push(`【都市网约车NSFW系统已激活】
当前为初次接载场景，乘客欲望阶段为"克制"。请在叙事中埋下暧昧氛围的种子：
- 描写乘客的外表细节（面容、穿着、香水气息、肢体语言），突出其吸引力
- 营造封闭车厢内的微妙张力（后视镜中的目光、沉默中的压迫感、气味的变化）
- 暗示乘客的内在状态（疲惫、紧张、警惕、隐藏的情绪波动），为后续互动提供入口
- 保持叙事质量的同时，让读者感受到"这个乘客不一般"的预感
这是欲望推进的起点，不需要直接发生NSFW行为，但必须让氛围向暧昧方向发展。
**重要：请在你的响应末尾输出 <都市网约车系统状态> 标签，包含乘客当前的醉酒/药物状态（如有）。**`);
  }

  if (行程类型) {
    组件.push(构建行程NSFW叙事约束(行程类型, 强度));
  }

  if (乘客欲望阶段 && 关系轨道) {
    组件.push(`【乘客欲望状态】阶段: ${乘客欲望阶段} | 关系轨道: ${关系轨道}
${乘客欲望阶段 === '克制' ? '乘客表面保持距离和礼貌，但内心可能已有微妙波动。描写重点：眼神回避中的偷瞄、身体语言的矛盾（如嘴上说不要但身体微微前倾）、欲言又止的对话。' : ''}
${乘客欲望阶段 === '试探' ? '乘客开始通过微妙的语言和动作试探司机的反应。描写重点：延长对话时间、不经意的身体靠近、在后视镜中刻意对视、话题从公事逐渐转向私人。' : ''}
${乘客欲望阶段 === '渴望' ? '乘客的欲望已经明显，但仍在压抑。描写重点：呼吸变得急促、身体不自觉地贴近、语言中带有暗示、对司机的触碰不再回避。' : ''}
${乘客欲望阶段 === '沉沦' ? '乘客已经主动，不再掩饰欲望。描写重点：主动发起身体接触、言语直接的暗示或请求、在车厢密闭空间中寻求亲密接触。' : ''}
${乘客欲望阶段 === '支配' ? '乘客完全主导互动节奏。描写重点：命令式的语言、权力反转、司机处于被动地位、特殊的场景和行为要求。' : ''}`);
  }

  if (暴露风险 !== undefined) {
    const 风险描述 = 暴露风险 >= 70 ? '高风险：随时可能被发现或曝光'
      : 暴露风险 >= 40 ? '中等风险：需要保持警惕'
      : '低风险：相对安全';
    组件.push(`【暴露风险】${暴露风险}% - ${风险描述}`);
  }

  if (醉酒状态 && 醉酒状态.等级 !== '清醒') {
    组件.push(构建醉酒叙事约束(醉酒状态, 强度));
  }

  if (药物状态 && 药物状态.生效阶段 !== '未生效') {
    组件.push(构建下药叙事约束(药物状态, 强度));
  }

  if (紧张度 !== undefined && 紧张度 > 0) {
    组件.push(构建行车记录仪紧张度约束(紧张度, 行车记录仪状态 ?? '录制中'));
  }

  if (后果) {
    组件.push(构建网约车后果叙事约束(后果.类型, 后果.严重程度, 后果.NPC信息));
  }

  组件.push(`【都市网约车系统状态输出要求】
当剧情中乘客欲望状态、暴露风险、紧张度、行程类型、药物/醉酒状态等发生变化时，必须在响应末尾输出以下格式的状态更新：
<都市网约车系统状态>
{"更新档案":{"NPC的ID":{"当前阶段":"阶段名","阶段进度":数字,"关系轨道":"轨道名","轨道进度":数字,"暴露风险值":数字,"紧张度":数字}}}
</都市网约车系统状态>

【可选字段说明】
- 当前行程类型: 当剧情中发生场景转换时可选指定，可选值: "深夜独处"|"后座暗示"|"停车场秘密"|"醉酒搭车"|"拼车暧昧"|"常客关系"|"行车记录仪"|"饮料下药"
- 当前地点: 当车辆移动到不同区域时可选指定，可选值: "城市主干道"|"深夜小巷"|"酒吧街"|"酒店区"|"地下停车场"|"高架桥"|"江边路"|"大学城"|"CBD写字楼"|"居民区"
- 药物状态: 当剧情中涉及饮料/药物时设置，格式: {"类型":"未知"|"迷药"|"安眠药"|"兴奋剂","生效阶段":"未生效"|"初期"|"强烈"|"衰退"}
- 醉酒状态: 当乘客饮酒时设置，格式: {"等级":"清醒"|"微醺"|"醉酒"|"烂醉","行为大胆度":数字,"记忆模糊度":数字,"判断力下降":布尔值}

【触发指引】
- 若剧情中出现"递饮料""请喝水""提供饮品"等情节，可设置 药物状态.生效阶段 为"初期"来暗示药物开始生效
- 若剧情中出现饮酒场景，可根据饮酒量设置 醉酒状态.等级
- 深夜时段+单一乘客+偏僻地点 适合触发"深夜独处"或"后座暗示"
- 到达目的地后乘客未离开 适合触发"停车场秘密"

- 只输出发生变化的NPC档案
- 阶段进度和轨道进度范围 0-100
- 暴露风险值和紧张度范围 0-100
- 如果没有发生任何状态变化，不需要输出此标签`);

  return 组件.join('\n\n');
};

// ==================== 互动选项提示词 ====================

/**
 * 构建互动选项提示词
 */
export const 构建互动选项提示词 = (
  欲望阶段: 乘客欲望阶段,
  关系轨道: 行程关系轨道,
  暴露风险: number,
  _已解锁互动: string[]
): string => {
  const 阶段可用选项: Record<乘客欲望阶段, string[]> = {
    '克制': ['保持专业距离', '简短寒暄', '通过后视镜礼貌回应'],
    '试探': ['延长对话', '调高空调温度', '选择更安静的路线'],
    '渴望': ['暧昧暗示', '肢体接触试探', '在后视镜中对视'],
    '沉沦': ['亲密互动', '延长行程时间', '在停车场停留'],
    '支配': ['主动掌控节奏', '深度亲密', '解锁特殊场景'],
  };

  const 轨道修正: Record<行程关系轨道, string> = {
    '纯爱': '侧重情感交流和心理互动',
    '暧昧': '侧重暧昧氛围和暗示行为',
    '肉体': '侧重身体接触和感官体验',
    '支配': '侧重权力动态和控制感',
    '交易': '侧重利益交换和权衡',
  };

  const 风险警告 = 暴露风险 >= 70 ? '\n⚠️ 当前暴露风险较高，选择需谨慎' : '';

  return `【互动选项】
当前欲望阶段: ${欲望阶段}
推荐选项: ${阶段可用选项[欲望阶段].join('、')}
关系轨道: ${关系轨道} - ${轨道修正[关系轨道]}
${风险警告}`;
};
