/**
 * 桌游社交 NSFW 提示词模块
 * 为 AI 叙事提供桌游叙事约束、紧张度叙事等组件
 */

import type { 桌游类型, 密室主题, NSFW编排模式 } from '../../models/boardGameNSFW/core';

// ==================== 桌游叙事约束 ====================

/**
 * 构建桌游叙事约束
 */
export const 构建桌游叙事约束 = (
  桌游类型: 桌游类型,
  密室主题?: 密室主题
): string => {
  const 桌游叙事: Record<桌游类型, string> = {
    '密室逃脱': `【密室逃脱 NSFW】
核心体验：黑暗、狭窄、限时带来的身体接触和依赖感。
- 强调环境氛围（黑暗、狭窄、限时压力）
- NPC反应真实：害怕时的依赖、兴奋时的主动
- 逃脱成功后的释放感转化为亲密${密室主题 ? `\n当前主题：${密室主题}` : ''}`,
    '狼人杀': `【狼人杀 NSFW】
核心体验：谎言、秘密、私下结盟的紧张感。
- 利用谎言和秘密的紧张感
- 私下结盟时的"交易"叙事
- 身份揭露后的情感冲击
- 出局后的独处安慰`,
    '剧本杀': `【剧本杀 NSFW】
核心体验："剧本"和"现实"的模糊。
- NPC在角色扮演中的"真情流露"
- 搜身/线索交换的自然过渡
- CP情感线现实化（假戏真做）
- 剧本高潮后的情感爆发`,
    '真心话大冒险': `【真心话大冒险 NSFW】
核心体验：众人围观下的隐私暴露和身体接触。
- 随机指令的不可预测性
- 酒后状态的"合理化"大胆行为
- 众人面前的紧张感和羞耻感`,
    '国王游戏': `【国王游戏 NSFW】
核心体验：无条件服从和随机指令的刺激感。
- 国王指令的随机性和不可拒绝性
- 屏风/帘子后的秘密亲密
- 众人视线范围内的暧昧动作`,
    '大富翁': `【大富翁 NSFW】
核心体验：掷骰子购地产业，"破产"意味着接受NSFW惩罚。
- 掷骰子的随机性制造不可预测的刺激感
- 地产=安全港，未购地产权=暴露于惩罚风险
- "金钱"惩罚转化为NSFW任务的等价交换
- NPC在"破产"边缘的紧张与期待交织
- 资产差距带来的权力动态：富有者发号施令，贫穷者被动接受`,
    '棋牌游戏': `【棋牌游戏 NSFW】
核心体验：手牌决定命运，虚张声势掩盖真实意图，胜负直接关联NSFW升级。
- 每局押注的NSFW指令增加期待感
- 虚张声势的心理博弈
- 连败累积惩罚：越输越深入，无法轻易退出
- 胜利者的奖励权利与失败者的服从义务
- 心理战阶段递进：试探（温和） → 博弈（加码） → 决战（全力）`,
    '骰子游戏': `【骰子游戏 NSFW】
核心体验：纯粹的随机性，骰子面决定行动，连续相同面产生累积效应。
- 每一掷都是不可逆的命运裁决
- 连续掷出相同面的"累积效应"：轻抚→亲吻→更深入
- 自定义骰子面可调整NSFW强度
- "豁免"面提供喘息，"翻倍"面制造极端刺激
- 骰子数量影响随机范围
- 历史记录制造"命运趋势"的叙事感`,
  };

  return 桌游叙事[桌游类型] || '';
};

// ==================== 完整桌游 NSFW 叙事约束 ====================

/**
 * 构建桌游 NSFW 完整叙事约束
 * 综合桌游类型、密室主题、紧张度、参与 NPC 生成统一 prompt 注入
 */
export const 构建桌游NSFW完整叙事约束 = (参数: {
  桌游类型?: 桌游类型;
  密室主题?: 密室主题;
  紧张度?: number;
  参与NPC摘要?: string;
}): string => {
  const { 桌游类型, 密室主题, 紧张度, 参与NPC摘要 } = 参数;

  const 组件: string[] = [];

  if (桌游类型) {
    组件.push(构建桌游叙事约束(桌游类型, 密室主题));
  }

  if (紧张度 !== undefined && 紧张度 > 0) {
    组件.push(构建桌游紧张度叙事约束(紧张度));
  }

  if (参与NPC摘要) {
    组件.push(`【参与桌游的 NPC 摘要】\n${参与NPC摘要}`);
  }

  return 组件.join('\n\n');
};

// ==================== 桌游紧张度叙事约束 ====================

/**
 * 构建桌游紧张度叙事约束
 */
export const 构建桌游紧张度叙事约束 = (紧张度: number): string => {
  let 描写指导 = '';
  if (紧张度 <= 30) {
    描写指导 = 'NPC表面正常，互动轻松自然。桌游氛围友好。';
  } else if (紧张度 <= 60) {
    描写指导 = 'NPC出现细微异常：偶尔脸红、眼神闪躲。周围人可能注意到但不确定。';
  } else if (紧张度 <= 85) {
    描写指导 = 'NPC明显紧张，说话结巴、面红耳赤。描写应强调随时被发现的紧迫感。';
  } else {
    描写指导 = 'NPC极度紧张，随时可能崩溃或暴露。描写应充满悬念和紧迫感。';
  }

  return `【桌游 NSFW 紧张度】
当前紧张度：${紧张度}/100
描写指导：${描写指导}

核心原则：在桌游规则的掩护下秘密推进亲密关系，"随时可能被发现"的紧张感是核心体验。`;
};

// ==================== 多人局叙事约束 ====================

/**
 * 构建多人局叙事约束
 */
export const 构建多人局叙事约束 = (参数: {
  参与人数: number;
  编排模式: NSFW编排模式;
  有阵营系统: boolean;
  有淘汰机制: boolean;
}): string => {
  const { 参与人数, 编排模式, 有阵营系统, 有淘汰机制 } = 参数;

  const 编排指导: Record<NSFW编排模式, string> = {
    '轮流': '指令按顺序轮流执行，每个 NPC 都有机会成为发起者和目标。描写应强调轮换中的期待感和被选中时的紧张感。',
    '随机': '指令随机分配，不可预测性极高。描写应强调骰子落定前的悬念和随机结果带来的意外刺激。',
    '阵营': 'NPC 按阵营分组，阵营内部合作、阵营之间对抗。描写应强调阵营忠诚与背叛的张力。',
  };

  const 阵营提示 = 有阵营系统
    ? '\n阵营系统已启用：注意描写阵营内部的秘密交流和阵营之间的对抗。出局 NPC 可能以旁观者身份继续影响局势。'
    : '';

  const 淘汰提示 = 有淘汰机制
    ? '\n淘汰机制已启用：NPC 可能中途出局，描写出局时的失落感和 remaining NPC 之间的微妙变化。'
    : '';

  return `【多人局 NSFW — ${参与人数}人局】
参与人数：${参与人数}人
编排模式：${编排模式}
${编排指导[编排模式]}
- 多人互动时应区分不同 NPC 的反应和个性表现
- 围观 NPC 的反应同样重要：脸红、偷笑、起哄、假装没看到${阵营提示}${淘汰提示}
- 描写应平衡每个 NPC 的戏份，避免某一方完全主导`;
};
