import { 提示词结构 } from '../../../types';

export const 构建同人剧情规划协议 = (): string => [
    '<同人剧情规划协议>',
    '# 【同人剧情规划协议】',
    '',
    '## 0. 边界与约束',
    '- 写入范围统一保持在 `同人剧情规划.*`；树外字段保持不写。',
    '- 同人剧情规划必须带原著锚点：每个任务、事件、镜头都必须能回答它承接的是哪一个分解组、对应的是哪一个原著事件。',
    '- 同人模式下，先对齐原著分解组，再决定当前章任务是延续原著、偏转原著、还是已形成分歧线。',
    '- 所有 string[] / number[] 字段必须保持数组写法。',
    '- 字段未知、结构不完整、证据不足时，按保守口径处理。',
    '',
    '## 1. 根字段',
    '- `同人剧情规划.当前对齐信息: 同人对齐信息结构`',
    '- `同人剧情规划.当前章目标: string[]`',
    '- `同人剧情规划.当前章任务: 同人剧情任务结构[]`',
    '- `同人剧情规划.分歧线: 同人分歧线结构[]`',
    '- `同人剧情规划.待触发事件: 同人待触发事件结构[]`',
    '- `同人剧情规划.镜头规划: 同人镜头结构[]`',
    '- `同人剧情规划.换组规则: 同人换组规则结构`',
    '',
    '## 2. 结构路径',
    '`同人剧情规划.当前对齐信息: { 当前分解组:number, 当前章节范围:string, 当前章节标题:string[], 当前承接方式:string, 当前原著状态:string[], 当前已形成偏转:string[] }`',
    '',
    '`同人剧情规划.当前章任务[i]: { 标题, 任务说明, 关联分解组:number[], 关联原著事件:string[], 保持不变的原著基线:string[], 当前偏转点:string[], 计划执行时间, 最早执行时间, 最晚执行时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 执行动作:string[], 完成判定:string[], 偏转后果:string[], 未偏转后果:string[], 完成后沉淀:string[], 当前状态 }`',
    '',
    '`同人剧情规划.分歧线[i]: { 分歧线名, 起点事件, 关联分解组:number[], 偏转原因:string[], 与原著不同之处:string[], 当前阶段:string, 影响范围:string[], 下一步扩大条件:string[], 回收条件:string[], 当前状态 }`',
    '',
    '`同人剧情规划.待触发事件[i]: { 事件名, 关联分解组:number[], 关联原著事件:string[], 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 触发后影响:string[], 错过后影响:string[], 若偏转则转入哪条分歧线:string[], 当前状态 }`',
    '',
    '`同人剧情规划.镜头规划[i]: { 镜头标题, 关联分解组:number[], 镜头内容, 触发时间, 触发条件:string[], 关联人物:string[], 关联地点:string[], 关联分歧线:string[], 作用:string[], 当前状态 }`',
    '',
    '`同人剧情规划.换组规则: { 当前组完成判定:string[], 下一组进入条件:string[], 禁止换组条件:string[], 换组后沉淀内容:string[], 换组后需清空字段:string[], 换组后需重建字段:string[] }`',
    '',
    '## 3. 对齐信息维护',
    '- `当前分解组` 必须与运行时记录的当前分解组保持一致。',
    '- `当前承接方式` 用于说明当前章是「延续原著」「偏转原著」还是「已形成分歧线」。',
    '- `当前原著状态` 描述当前对齐原著的进度状态。',
    '- `当前已形成偏转` 列出本章已确认的偏转点，供后续组参考。',
    '',
    '## 4. 任务纪律',
    '- 每个 `当前章任务` 必须明确回答：它对应哪个分解组、哪段原著事件。',
    '- 若任务与原著一致，写入 `保持不变的原著基线`。',
    '- 若任务已偏转，写入 `当前偏转点` 并在 `分歧线` 中创建或更新对应条目。',
    '- 任务必须有明确的 `完成判定`，不能以「差不多」含糊带过。',
    '- `换组规则` 中的 `当前组完成判定` 必须可验证，不是概括性描述。',
    '',
    '## 5. 分歧线维护',
    '- `分歧线` 是同人规划的核心字段，不是附加备注。',
    '- 每条分歧线必须回答：起点事件、偏转原因、与原著不同之处、当前阶段。',
    '- 分歧线扩大需要明确条件，不是无限蔓延。',
    '- 分歧线回收也需要明确条件。',
    '',
    '</同人剧情规划协议>'
].join('\n');

export const 运行时_同人剧情规划Schema: 提示词结构 = {
    id: 'runtime_fandom_story_plan_schema',
    标题: '同人剧情规划Schema',
    内容: 构建同人剧情规划协议(),
    类型: '运行时Schema',
    启用: false
};
