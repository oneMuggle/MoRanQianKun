/**
 * 共享COT片段库
 * 提供跨运行时链路复用的通用思维链片段
 */

import { 提示词结构 } from '../../types';

/**
 * 判定逻辑共享片段
 * 适用于所有需要判定计算的场景
 */
const 共享_判定逻辑 = `
判断优先级：
1. 检查前置条件是否满足（属性门槛、资源充足性、状态要求）
2. 计算基础值（主属性 + 辅助属性 + 境界修正）
3. 叠加随机因子（R值范围 -18 ~ +18）
4. 与难度等级（DC）比对
5. 结算结果（大成功/成功/失败/大失败）
6. 根据结果更新状态（变量落点）
`;

/**
 * 资源校验共享片段
 * 适用于资源消耗、获得、不足检测等场景
 */
const 共享_资源校验 = `
资源处理流程：
1. 验证资源充足性（精力、内力、饱腹、口渴、金钱等）
2. 资源不足时给出明确提示，不执行消耗
3. 资源充足时计算消耗/获得量
4. 执行状态变更（add/set 操作）
5. 记录审计日志（用于追踪变化来源）
`;

/**
 * NPC行为准则共享片段
 * 适用于NPC互动、对话、决策等场景
 */
const 共享_NPC行为 = `
NPC行为准则：
1. 保持人设一致性（性格、身份、背景决定行为模式）
2. 根据关系亲密度调整态度（陌生人/熟人/友人/恋人等）
3. 遵守世界观基本逻辑（武力梯度、社会规范、时代背景）
4. 不泄露未公开信息（秘密、幕后行动、隐藏关系）
5. 允许拒绝、质疑、讨价还价、延后执行
`;

/**
 * 时间推进共享片段
 * 适用于回合结束、时间流逝等场景
 */
const 共享_时间推进 = `
时间推进规则：
1. 根据行动耗时推进环境时间（时/分）
2. 跨天时分进位（日/月/年）
3. 时间推进应与正文描述的动作耗时一致
4. 保持时间快照可追溯
`;

/**
 * 变量落点共享片段
 * 适用于变量变化、状态更新等场景
 */
const 共享_变量落点 = `
变量落点规范：
1. <变量规划>只记录本回合已经成立的变化
2. 用自然语言说明"发生了什么/涉及谁/为什么成立/后续应落到哪些状态"
3. 不写伪命令或伪JSON
4. 不预设固定分类或书写顺序
5. 变化必须有正文依据支撑
`;

/**
 * 世界观一致性检查共享片段
 * 适用于新内容生成、世界演变等场景
 */
const 共享_世界观一致性 = `
世界观一致性检查：
1. 武力梯度一致性（境界高者不应被境界低者轻易击败）
2. 社会规范一致性（时代背景决定行为边界）
3. 地理/空间一致性（地点、距离、势力范围）
4. 逻辑一致性（因果关系、动机合理）
`;

/**
 * 战斗判定共享片段
 * 适用于战斗场景的各种判定
 */
const 共享_战斗判定 = `
战斗判定流程：
1. 确认战斗阶段（先机/瞄准/接战/防御/伤害/态势）
2. 计算攻击方判定值（力量/敏捷 + 境界修正 + 装备加成 + R值）
3. 计算防御方判定值（敏捷/根骨 + 境界修正 + 装备加成 + R值）
4. 对抗判定：攻击值 vs 防御值 + 难度
5. 结算伤害（成功减半/大失败无效/成功正常/大成功加强）
6. 更新部位生命值
`;

/**
 * 记忆与上下文共享片段
 * 适用于记忆调用、上下文管理等场景
 */
const 共享_记忆管理 = `
记忆管理规范：
1. 优先使用即时记忆（浮光掠影）处理当前回合
2. 短期记忆（浅层识海）用于本章节内承接
3. 中期记忆（深层识海）用于跨章节关联
4. 长期记忆（神魂烙印）用于世界观级别人物/事件
5. 信息可见性分析：谁已知道/谁仍不知/谁误会/谁只在读者视角成立
`;

/**
 * 导出为提示词结构数组
 */
export const 共享COT片段库: 提示词结构[] = [
    {
        id: 'shared_cot_judgment',
        标题: '共享COT-判定逻辑',
        内容: 共享_判定逻辑,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_resource',
        标题: '共享COT-资源校验',
        内容: 共享_资源校验,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_npc',
        标题: '共享COT-NPC行为',
        内容: 共享_NPC行为,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_time',
        标题: '共享COT-时间推进',
        内容: 共享_时间推进,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_variable',
        标题: '共享COT-变量落点',
        内容: 共享_变量落点,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_world',
        标题: '共享COT-世界观一致性',
        内容: 共享_世界观一致性,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_combat',
        标题: '共享COT-战斗判定',
        内容: 共享_战斗判定,
        类型: '核心设定',
        启用: true
    },
    {
        id: 'shared_cot_memory',
        标题: '共享COT-记忆管理',
        内容: 共享_记忆管理,
        类型: '核心设定',
        启用: true
    }
];

// 单独导出方便按需使用
export {
    共享_判定逻辑,
    共享_资源校验,
    共享_NPC行为,
    共享_时间推进,
    共享_变量落点,
    共享_世界观一致性,
    共享_战斗判定,
    共享_记忆管理
};
