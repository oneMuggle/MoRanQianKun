# storyResponseParser 重复嵌套函数提取

> **Commit:** `4d8c78a` | **Status:** ✅ 已完成 | **Date:** 2026-04-28

## 概述

将 `storyResponseParser.ts` 中三个重复的嵌套函数提取为模块级函数，代码减少 168 行（-72%）。

## 已完成内容

### 提取的函数

以下三个函数在故事响应解析器中嵌套重复了 3 次，现已提取为模块级函数：

1. **`清理命令尾部分隔符`** — 移除命令值的尾部分隔符
2. **`计算括号平衡`** — 计算括号嵌套层级以辅助解析
3. **`收集多行命令值`** — 解析过程中收集多行命令值

### 前后对比

| 指标 | 修改前 | 修改后 | 变化 |
|--------|--------|-------|--------|
| 文件大小 | 233 行 | 65 行 | -72% |
| 重复函数 | 3 份 x 3 处 | 各 1 份 | 已消除 |
| 嵌套深度 | 4+ 层 | 减少 | 可读性提升 |

**状态:** ✅ 已完成

## 变更文件

- `services/ai/text/storyResponseParser.ts`（-168 行，+65 行）

## 待办事项

- [ ] 为三个已提取的函数添加单元测试（现已是模块级，适合测试）
- [ ] 检查 `services/ai/text/` 下其他文件是否存在类似重复模式

## 备注

- 重构使解析函数更容易测试，因为函数现在在模块级别而非嵌套在其他函数内部。
