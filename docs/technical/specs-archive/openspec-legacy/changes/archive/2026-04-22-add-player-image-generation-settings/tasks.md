# 主角生图设置优化 - 实现任务

## 1. 数据结构扩展

- [x] 1.1 在 `models/system.ts` 的 `功能模型占位配置结构` 中添加主角生图字段
  - 主角生图启用: boolean
  - 主角生图独立接口启用: boolean
  - 主角生图后端类型: 文生图后端类型
  - 主角生图模型使用模型: string
  - 主角画师串预设ID: string
  - 主角PNG画风预设ID: string
  - 主角词组转化器预设ID: string

- [x] 1.2 添加 `主角生图相关字段` 的类型定义（如需要）

**完成标准**: 类型定义编译通过，无 TypeScript 错误

## 2. 设置面板扩展

- [x] 2.1 在 `ImageGenerationSettings.tsx` 中添加 `player` 页签类型
- [x] 2.2 在基础页签选项中添加「主角」页签按钮
- [x] 2.3 创建 `renderPlayerPage()` 渲染函数
- [x] 2.4 添加主角画师串预设的作用域支持
- [x] 2.5 添加主角PNG画风预设的作用域支持
- [x] 2.6 添加主角词组转化器预设的作用域支持

**完成标准**: 设置面板可正常渲染主角页签，各选项可选择并保存 - ✅

**完成标准**: 设置面板可正常渲染主角页签，各选项可选择并保存

## 3. 角色锚点管理扩展

- [x] 3.1 在预设页签中添加「适用范围：主角」选项
- [x] 3.2 在设置面板中添加主角锚点操作入口
- [x] 3.3 连接锚点操作到工作流

**完成标准**: 可在设置面板提取和查看主角锚点

## 4. 测试与验证

- [x] 4.1 保存设置后刷新，检查主角配置是否正确持久化

- [x] 4.2 验证主角生图使用独立配置（非NPC配置）

- [x] 4.3 验证向后兼容（旧存档中主角生图复用NPC配置）

**完成标准**: 所有测试场景通过

## 5. 移动端适配（如需要）

- [x] 5.1 检查 `MobileSettingsModal.tsx` 是否需要同步更新

- [x] 5.2 如需要，创建对应的移动端主角页签

**完成标准**: 移动端设置面板功能完整