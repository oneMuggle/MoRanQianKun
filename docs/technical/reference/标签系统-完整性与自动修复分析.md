# 墨染乾坤 - 标签完整性检查与自动修复机制分析

本文档基于代码调研结果，整理项目中标签（tag/label）的实现、完整性检查和自动修复机制。

---

## 一、标签类型体系

### 1.1 协议标签（XML风格）

**定义位置**: `services/ai/text/storyResponseParser.ts`

```typescript
const 协议标签列表 = ['thinking', '剧情规划', '变量规划', '正文', '短期记忆', '命令', '行动选项', '动态世界', 'judge'] as const;
const 协议固定必填标签 = ['正文', '短期记忆'] as const;
```

| 标签 | 必填 | 说明 |
|------|------|------|
| `<正文>` | ✅ | 主剧情内容，三类合法行：旁白、台词、判定 |
| `<短期记忆>` | ✅ | 本回合事实摘要，100字内 |
| `<thinking>` | 可选 | 思考过程 |
| `<变量规划>` | 可选 | 变量变化说明 |
| `<剧情规划>` | 可选 | 后续承接摘要 |
| `<行动选项>` | 可选 | 玩家可选行动 |
| `<judge>` | 可选 | 判定子结构，仅存在于正文内部 |

### 1.2 画师标签

**定义位置**: `services/ai/artistTagDictionary.ts`

```typescript
export const 内置画师标签词库: string[] = [
    'abec', 'aoki hagane', 'ask', 'bob', 'carnelian',
    'digitalflyer', 'fred perry', 'hiten', 'hisasi', 'hong',
    'kantoku', 'karory', 'kurehito misaki', 'lack',
    'mika pikazo', 'mishima kurone', 'morikura en',
    'nekosokku okuma', 'pako', 'popman3580', 'ranfren',
    'redjuice', 'saitom', 'sayori', 'swd3e2', 'toi8',
    'torino aqua', 'wlop', 'yaegashi nan', 'yoneyama mai',
    'yoshiku', 'yuumei'
];
```

**解析位置**: `services/ai/artistTagExtractor.ts`

---

## 二、标签完整性检查

### 2.1 核心函数

**文件**: `services/ai/text/storyResponseParser.ts`

**函数**: `检测标签完整性问题` (行 438-463)

```typescript
const 检测标签完整性问题 = (text: string, _options: Required<StoryParseOptions>): string[] => {
    const issues: string[] = [];
    const thinkingSegment = 提取首尾思考区段(text);
    const textForValidation = thinkingSegment.matched ? thinkingSegment.textWithoutThinking : text;
    const requiredTags = [...协议固定必填标签];

    for (const tag of requiredTags) {
        const stats = 统计标签数量(textForValidation, tag);
        if (stats.open === 0) {
            issues.push(`缺少 <${tag}> 开始标签`);
        }
        if (stats.close === 0) {
            issues.push(`缺少 </${tag}> 闭合标签`);
        }
        if (stats.open !== stats.close) {
            issues.push(`<${tag}> 开闭标签数量不一致（${stats.open}/${stats.close}）`);
        }
        if (stats.open > 0 && stats.close > 0) {
            const payloads = 提取标签载荷列表_含空内容(textForValidation, tag);
            if (payloads.length === 0 || payloads.every(item => item.length === 0)) {
                issues.push(`<${tag}> 标签内容为空`);
            }
        }
    }
    return issues;
};
```

### 2.2 检查内容

| 检查项 | 判定规则 |
|--------|----------|
| 缺少开始标签 | `统计标签数量(tag).open === 0` |
| 缺少闭合标签 | `统计标签数量(tag).close === 0` |
| 标签数量不一致 | `open !== close` |
| 标签内容为空 | 所有 payload 长度之和为 0 |

### 2.3 触发条件

**文件**: `services/ai/text/storyResponseParser.ts`

**入口**: `parseStoryRawText` (行 1199-1212)

```typescript
export const parseStoryRawText = (content: string, options?: StoryParseOptions): GameResponse => {
    const parseOptions = 规范化解析选项(options || 默认解析选项);
    // ... 修复逻辑 ...
    if (parseOptions.validateTagCompleteness) {
        const issues = 检测标签完整性问题(normalizedText, parseOptions);
        if (issues.length > 0) {
            const detail = `标签完整性校验失败：\n- ${issues.join('\n- ')}`;
            throw new StoryResponseParseError(detail, rawText, detail);
        }
    }
    // ...
};
```

### 2.4 配置开关

```typescript
const 默认解析选项: Required<StoryParseOptions> = {
    validateTagCompleteness: false,  // 默认关闭
    enableTagRepair: true,             // 默认开启
    requireActionOptionsTag: false,
    requireDynamicWorldTag: false
};

// 构建方式
const 构建标签解析选项 = (config: 游戏设置结构) => ({
    validateTagCompleteness: config?.启用标签检测完整性 === true,
    enableTagRepair: config?.启用标签修复 !== false,
});
```

---

## 三、自动修复机制

### 3.1 修复思考区后半段

**函数**: `修复思考区后半段标签协议文本` (行 1083-1101)

```typescript
const 修复思考区后半段标签协议文本 = (sourceText: string): string => {
    const source = typeof sourceText === 'string' ? sourceText : '';
    if (!source) return '';

    const thinkingSegment = 提取首尾思考区段(source);
    if (!thinkingSegment.matched) {
        return 修复标签协议文本(source);
    }

    const tail = thinkingSegment.textWithoutThinking;
    if (!tail) {
        return source;
    }

    const prefixLength = Math.max(0, source.length - tail.length);
    const prefix = source.slice(0, prefixLength);
    const repairedTail = 修复标签协议文本(tail);
    return `${prefix}${repairedTail}`;
};
```

**作用**: 分离 thinking 区和正文区，对正文区单独进行标签修复。

### 3.2 修复标签协议文本

**函数**: `修复标签协议文本` (行 465-551)

```typescript
const 修复标签协议文本 = (content: string): string => {
    const text = 归一化标签括号符号(content || '');  // 1. 归一化括号
    if (!text.trim()) return text;

    const tagRegex = /<\s*(\/?)\s*([A-Za-z0-9_\-\u3400-\u9fff]+)\s*>/g;
    const stack: string[] = [];
    let lastIndex = 0;
    let rebuilt = '';

    // 2. 遍历标签，使用栈结构修复嵌套
    // ...
    // 3. 补齐未闭合标��
    while (stack.length > 0) {
        rebuilt += `</${stack.pop() as string}>`;
    }
    // 4. 补全缺失区块
    return 补全协议缺失区块(rebuilt);
};
```

**修复步骤**:

1. **归一化括号符号**: `＜／＞` → `< />`
2. **解析标签建立栈**: 遍历 XML 标签，构建嵌套关系
3. **修复未闭合标签**: 对缺失的闭合标签进行补齐
4. **补全缺失区块**: 确保 `<正文>` 和 `<短期记忆>` 存在

### 3.3 补全缺失区块

**函数**: `补全协议缺失区块` (行 354-374)

```typescript
const 补全协议缺失区块 = (content: string): string => {
    let text = content || '';
    const sections = 提取标题区块内容(text);
    const 正文候选 = sections.正文 || 提取候选正文文本(text);
    const 短期候选 = sections.短期记忆 || '';

    text = 写入协议标签段(text, '正文', 正文候选, { 允许空内容: true });
    text = 写入协议标签段(text, '短期记忆', 短期候选, { 缺失时默认内容: '无' });
    // ...
    return text;
};
```

**保证**: 输出文本中至少存在 `<正文>` 和 `<短期记忆>` 区块。

---

## 四、错误处理

### 4.1 异常类

**文件**: `services/ai/text/storyResponseParser.ts`

```typescript
export class StoryResponseParseError extends Error {
    rawText: string;
    parseDetail?: string;

    constructor(message: string, rawText: string, parseDetail?: string) {
        super(message);
        this.name = 'StoryResponseParseError';
        this.rawText = rawText;
        this.parseDetail = parseDetail;
    }
}
```

### 4.2 错误触发点

| 触发点 | 条件 |
|--------|------|
| `parseStoryRawText` 行 1206-1211 | `validateTagCompleteness === true` 且 `issues.length > 0` |
| `parseStoryRawText` 行 1233-1236 | 缺少 `<正文>` 有效内容 |
| JSON 解析失败 | 返回内容结构不完整 |

---

## 五、相关文件索引

### 核心文件

| 文件 | 职责 |
|------|------|
| `services/ai/text/storyResponseParser.ts` | 标签解析、完整性检查、自动修复 |
| `services/ai/artistTagExtractor.ts` | 画师标签提取 |
| `services/ai/artistTagDictionary.ts` | 画师标签词库 |
| `utils/jsonRepair.ts` | JSON 片段修复 |

### 配置位置

| 文件 | 位置 |
|------|------|
| 启用标签修复 | `hooks/useGame.ts` - `构建标签解析选项` |
| 启用完整性校验 | 应用设置 - `启用标签检测完整性` |

---

## 六、调用链

```
AI回复原始文本
    ↓
parseStoryRawText(content, options)
    ↓
规范化解析选项(options)
    ↓
[enableTagRepair === true]
    ↓ 修复思考区后半段标签协议文本
    ↓ 修复标签协议文本
    ↓ 补全协议缺失区块
    ↓
[validateTagCompleteness === true]
    ↓ 检测标签完整性问题
    ↓ 抛出 StoryResponseParseError（如有问题）
    ↓
解析标签协议响应 (GameResponse)
    ↓
返回结构化结果
```

---

## 七、使用建议

### 7.1 开启/关闭修复

```typescript
// 关闭自动修复
parseStoryRawText(content, { enableTagRepair: false });

// 开启完整性校验
parseStoryRawText(content, { validateTagCompleteness: true });
```

### 7.2 调试完整性问题

```typescript
try {
    const result = parseStoryRawText(content, {
        validateTagCompleteness: true,
        enableTagRepair: false  // 关闭修复以查看原始问题
    });
} catch (e) {
    if (e instanceof StoryResponseParseError) {
        console.log(e.parseDetail);  // 打印问题清单
    }
}
```

---

*本文档最后更新于 2026年4月*