# 墨染江湖 - 夜间实施方案

> 生成日期: 2026-05-11 23:00
> 执行窗口: 23:00 - 08:00
> 分析范围: docs/plans/*.md, npm run build/lint, git status

---

## 1. 项目状态概览

### 1.1 代码库基本信息

| 指标 | 数值 |
|------|------|
| 总文件数 | 329 (不含 node_modules/dist) |
| 代码行数 | ~99k 行 TypeScript/TSX |
| 构建状态 | ✅ 通过 (10.25s) |
| Lint状态 | ✅ 0 errors, 2021 warnings (仅命名规范) |
| Git状态 | 本地 main 领先云端 (5a9f12c)，但 origin/main 已前移 |

### 1.2 写真NSFW模块现状

| 子模块 | 状态 | 文件 |
|--------|------|------|
| types.ts | ✅ 完成 | models/photographyNSFW/types.ts |
| states.ts | ✅ 完成 | models/photographyNSFW/states.ts |
| index.ts (桶导出+设置) | ✅ 完成 | models/photographyNSFW/index.ts |
| engine.ts | ✅ 完成 (470行) | hooks/useGame/photographyNSFWEngine.ts |
| integration.ts | ✅ 完成 (112行) | hooks/useGame/photographyNSFWIntegration.ts |
| 设置UI | 🔲 待实施 | components/features/Settings/PhotographyNSFWSettings.tsx |
| system.ts接入 | 🔲 待实施 (Step 6, +5行) | models/system.ts |
| useGame.ts初始化 | 🔲 待实施 (Step 7, +30行) | hooks/useGame.ts |
| sendWorkflow接入 | 🔲 待实施 (Step 9, +15行) | hooks/useGame/sendWorkflow/index.ts |
| responseCommandProcessor | 🔲 待实施 (Step 10, +25行) | hooks/useGame/responseCommandProcessor.ts |

### 1.3 云端同步状态

origin/main 已前移，本地落后。Push 被拒绝。需要先 fetch + rebase 或 force push。
**处理方式**: 先完成本地实现，再处理云端同步。

---

## 2. 夜间任务

### 任务: 完成写真NSFW Phase 2 (Steps 6-10)

**目标**: 将已完成的 Phase 1 (types/engine/integration) 接入主系统。

### Step 6: 修改 system.ts (+5行)
- 导入 `写真NSFW设置` 和 `写真系统扩展` 类型
- 在 `游戏设置结构` 接口中添加 `写真NSFW设置?` 字段
- 在主游戏状态接口中添加 `写真系统?` 字段

### Step 7: 接入 useGame.ts 初始化 (+30行)
- 添加 useEffect 初始化 `写真系统` 状态
- 条件: `gameConfig?.写真NSFW设置?.启用写真NSFW系统 === true` 且 `时代配置ID` 以 `contemporary_` 开头

### Step 8: integration.ts 已有 (112行完成)

### Step 9: 接入 sendWorkflow (+15行)
- 导入 `构建写真NSFW参数`
- 在提示词构建段调用并合并到参数

### Step 10: 接入 responseCommandProcessor (+25行)
- 解析 `<写真系统状态>` 标签
- 应用更新到 `state.写真系统`

### Step 5: 设置UI组件
- 创建 `components/features/Settings/PhotographyNSFWSettings.tsx`
- 参考 `campusNSFWSettings.tsx` 模式

---

## 3. 执行约束

- 如果任务失败，记录到 `.night-work/skipped.md`
- 完成后构建验证: `npm run build`
- 提交信息: `feat(photography-nsfw): complete Phase 2 integration (Steps 5-10)`
- 更新 `.night-work/done.md`

---

## 4. 云端同步策略

由于 origin/main 已前移且网络超时，push 会被拒绝。解决方案:
1. `git push --force` (风险: 覆盖云端) 或
2. 先 fetch origin 再 rebase (需要网络，当前超时)

**临时策略**: 本地提交完成，如果 push 超时则记录到 done.md，下次同步。
