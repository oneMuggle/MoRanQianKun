# 世界观武力等级分级修改方案

## 一、现状分析

### 当前世界配置 (`WorldGenConfig`)

```typescript
interface WorldGenConfig {
    worldName: string;
    worldSize: '弹丸之地' | '九州宏大' | '无尽位面';
    dynastySetting: string;
    sectDensity: '稀少' | '适中' | '林立';
    tianjiaoSetting: string;        // 天骄设定（自由文本）
    worldExtraRequirement: string;
    manualWorldPrompt: string;
    manualRealmPrompt: string;
    difficulty: 游戏难度;          // relaxed | easy | normal | hard | extreme
}
```

### 存在的问题

| 问题 | 说明 |
|------|------|
| **缺乏武力等级** | `tianjiaoSetting` 是自由文本，无法标准化控制世界观强度 |
| **低武/中武/高武未区分** | 无法在开局时明确设定世界的武力上限 |
| **修仙未支持** | 当前仅针对武侠，无法选择修仙或玄幻 |
| **提示词生成依赖额外要求** | 需要用户手动在 `worldExtraRequirement` 填写，门槛高 |

---

## 二、修改方案

### 2.1 新增类型定义

**修改文件**: `models/system.ts`

```typescript
// 新增：武力等级类型
export type 武力等级 = '低武' | '中武' | '高武' | '修仙';

// 新增：武力等级配置结构
export interface 武力等级配置结构 {
    武力等级: 武力等级;
    武力描述: string;              // 供 AI 理解的详细描述
}

// 武力等级预设描述
export const 武力等级描述映射: Record<武力等级, string> = {
    '低武': '武侠初期，拳脚功夫为主，刀剑为辅。高手亦不过力大招猛、身法矫健，止步于人体极限。江湖以招式精妙、经验丰富者为强，内力可有但浅薄。无轻功飞纵，无隔空伤人，无真气外放。',
    '中武': '武侠中期，内力显著，可登堂入室。高手能以一敌十，轻功可腾跃数丈，暗器可及远。江湖以门派传承、功法境界为尊。或有真传秘籍之争，或有宗师独步一方。',
    '高武': '武侠巅峰/后期化境，宗师辈出，可开宗立派。高手段已臻化境，飞花摘叶皆可伤人，真气外放亦有之。秘籍、名器、传承皆为稀缺资源。高境界者凤毛麟角，百年难遇。',
    '修仙': '超越武侠范畴，涉及灵气、真元、阵法、炼丹、灵兽等元素。修行者可御器飞行、夺天地造化。门派以修仙宗门为主，凡人江湖缩小为世俗一隅。秘境、灵脉、仙缘为修行关键。'
};

// 修改 WorldGenConfig 接口
export interface WorldGenConfig {
    worldName: string;
    worldSize: '弹丸之地' | '九州宏大' | '无尽位面';
    dynastySetting: string;
    sectDensity: '稀少' | '适中' | '林立';
    tianjiaoSetting: string;
    武力等级?: 武力等级;          // 新增：可选字段，兼容旧存档
    worldExtraRequirement: string;
    manualWorldPrompt: string;
    manualRealmPrompt: string;
    difficulty: 游戏难度;
}
```

### 2.2 UI 选项配置

**修改文件**: `components/features/NewGame/NewGameWizard.tsx`

```typescript
// 新增：武力等级下拉选项
const 武力等级下拉选项: Array<{ value: 武力等级; label: string; hint: string }> = [
    { value: '低武', label: '低武 (凡尘江湖)', hint: '拳脚刀剑，人体极限，无真气外放' },
    { value: '中武', label: '中武 (门派林立)', hint: '内功深厚，轻功飞纵，暗器及远' },
    { value: '高武', label: '高武 (宗师时代)', hint: '化境高手，真气外放，秘籍稀缺' },
    { value: '修仙', label: '修仙 (仙凡两界)', hint: '灵气真元，御器飞行，秘境仙缘' }
];

// 默认状态修改
const [worldConfig, setWorldConfig] = useState<WorldGenConfig>({
    worldName: '太古界',
    worldSize: '九州宏大',
    dynastySetting: '群雄逐鹿，王朝末年',
    sectDensity: '林立',
    tianjiaoSetting: '大争之世，天骄并起',
    武力等级: '中武',              // 新增默认值
    worldExtraRequirement: '',
    manualWorldPrompt: '',
    manualRealmPrompt: '',
    difficulty: 'normal'
});

// UI 中添加武力等级选择器（在宗门密度下方）
<div className="space-y-2">
    <label className="text-sm text-wuxia-cyan font-bold">武力等级</label>
    <InlineSelect
        value={worldConfig.武力等级 || '中武'}
        options={武力等级下拉选项.map(o => ({ value: o.value, label: o.label }))}
        onChange={(武力等级) => setWorldConfig({ ...worldConfig, 武力等级 })}
    />
    <p className="text-xs text-gray-500">
        {武力等级下拉选项.find(o => o.value === (worldConfig.武力等级 || '中武'))?.hint}
    </p>
</div>
```

### 2.3 提示词构建修改

**修改文件**: `prompts/runtime/worldSetup.ts`

```typescript
import { 武力等级描述映射, type 武力等级 } from '../../models/system';

export const 构建世界观锚点提示词 = (worldConfig: WorldGenConfig, charData: 角色数据结构): string => `
【当前存档世界锚点（World Bible Anchor）】
- 世界名称: ${worldConfig.worldName}
- 世界规模: ${worldConfig.worldSize}
- 武力等级: ${worldConfig.武力等级 || '中武'}
- 武力描述: ${武力等级描述映射[worldConfig.武力等级 as 武力等级] || 武力等级描述映射['中武']}
- 宗门密度: ${worldConfig.sectDensity}
- 天骄设定: ${worldConfig.tianjiaoSetting}
- 游戏难度: ${worldConfig.difficulty || 'normal'}
- 世界观额外要求: ${worldConfig.worldExtraRequirement?.trim() || '无'}

【世界母本硬边界】
- 本存档世界观必须兼容武侠成长主轴。
- 世界应是长期母本，不是开场剧情、任务提纲或玩家专属设定。
- 高手、秘籍、名器、传承、危险区域必须有合理稀缺度与风险代价。
- 世界保持风险与回报匹配，不写成低风险高回报的福利场。
- **武力等级约束**: ${武力等级描述映射[worldConfig.武力等级 as 武力等级] || 武力等级描述映射['中武']}
- ${worldConfig.武力等级 === '修仙' ? '本世界为修仙世界观，可包含灵气、真元、阵法、灵兽等元素，门派以修仙宗门为主。' : ''}
...
`;
```

### 2.4 世界生成提示词修改

**修改文件**: `prompts/runtime/worldGeneration.ts`

```typescript
// 在【world_prompt 必含信息】中添加武力等级约束
export const 世界观生成系统提示词 = `
...
【world_prompt 必含信息】
1. 世界总览
- 世界名称
- 时代基调
- 地理尺度
- 社会秩序
- 主导文明结构
- **武力等级**: ${worldConfig.武力等级 || '中武'} - ${武力等级描述映射[worldConfig.武力等级 as 武力等级]}

2. 势力版图
...

4. 风险生态
- 主要冲突源头
- 危险区域与对应风险类型
${worldConfig.武力等级 === '修仙' ? '- 修仙元素：灵气分布、秘境位置、灵脉等级' : ''}
- 典型生存压力（财货、伤病、名声、仇怨、资源稀缺）

5. 成长体系边界
- **必须明确本世界的武力边界**，且与项目当前"累计境界值"口径兼容。
- ${武力等级描述映射[worldConfig.武力等级 as 武力等级] || 武力等级描述映射['中武']}
${worldConfig.武力等级 === '修仙' ? '- 修仙边界：本世界允许灵气修炼、御器飞行、阵法符箓，但禁止仙界飞升、神格降临等超规格能力。' : ''}
...
`;
```

### 2.5 存档兼容性处理

**修改文件**: `utils/customNewGamePresets.ts`

```typescript
export const 标准化开局预设方案 = (raw: any): 开局预设方案结构 | null => {
    // ...
    return {
        id,
        名称,
        简介: 标准化文本(raw.简介) || '自定义开局方案',
        worldConfig: {
            worldName: 标准化文本(raw?.worldConfig?.worldName),
            worldSize: /* ... */,
            dynastySetting: 标准化文本(raw?.worldConfig?.dynastySetting),
            sectDensity: /* ... */,
            tianjiaoSetting: 标准化文本(raw?.worldConfig?.tianjiaoSetting),
            武力等级: raw?.worldConfig?.武力等级 === '低武' || raw?.worldConfig?.武力等级 === '中武' || raw?.worldConfig?.武力等级 === '高武' || raw?.worldConfig?.武力等级 === '修仙'
                ? raw.worldConfig.武力等级
                : '中武',  // 默认值
            difficulty: /* ... */,
            worldExtraRequirement: 标准化文本(raw?.worldConfig?.worldExtraRequirement),
            manualWorldPrompt: 标准化文本(raw?.worldConfig?.manualWorldPrompt),
            manualRealmPrompt: 标准化文本(raw?.worldConfig?.manualRealmPrompt)
        },
        // ...
    };
};
```

---

## 三、修改文件清单

| 序号 | 文件路径 | 修改内容 |
|------|---------|---------|
| 1 | `models/system.ts` | 新增 `武力等级` 类型和描述映射，修改 `WorldGenConfig` |
| 2 | `components/features/NewGame/NewGameWizard.tsx` | 添加武力等级下拉选项和状态处理 |
| 3 | `prompts/runtime/worldSetup.ts` | 在锚点提示词中添加武力等级信息 |
| 4 | `prompts/runtime/worldGeneration.ts` | 在生成提示词中添加武力等级约束 |
| 5 | `utils/customNewGamePresets.ts` | 标准化处理新字段 |
| 6 | `types.ts` (如需要) | 导出新类型 |

---

## 四、武力等级效果对照表

| 武力等级 | 典型特征 | 高手表现 | 武功上限 | 秘籍稀缺度 | 建议难度 |
|---------|---------|---------|---------|-----------|---------|
| **低武** | 拳脚刀剑，人体极限 | 力大招猛，身法矫健 | 以一敌十为极限 | 高（普通门派已是传承） | easy/normal |
| **中武** | 内功深厚，门派林立 | 登堂入室，宗师雏形 | 以一敌百为极限 | 中（名门大派有真传） | normal/hard |
| **高武** | 化境高手，开宗立派 | 真气外放，飞花摘叶 | 百年难遇一高手 | 低（秘籍名器皆为传说） | hard/extreme |
| **修仙** | 灵气真元，仙凡两界 | 御器飞行，夺天造化 | 凡人之巅有仙缘 | 极低（仙缘可遇不可求） | extreme |

---

## 五、境界体系联动

当 `武力等级 = '修仙'` 时，应自动触发以下调整：

### 5.1 境界体系提示词

自动注入修仙相关内容到 `core_realm`：
```
【修仙补充】
- 灵气：修行根基，天地间弥漫的能量
- 真元：灵气凝练后的修行者内力
- 筑基/金丹/元婴等：修仙境界层次
- 灵根：修行资质，先天禀赋
```

### 5.2 世界生成提示词

修仙模式下自动包含：
- 灵气分布图
- 灵脉位置
- 秘境入口
- 仙门宗门设定
- 凡俗江湖定位

### 5.3 实现方式

在 `worldGenerationWorkflow.ts` 中检测 `武力等级`：

```typescript
const useManualWorldPrompt = normalizedManualWorldPrompt.length > 0;
const useManualRealmPrompt = 启用修炼体系 && normalizedManualRealmPrompt.length > 0;
const isImmortalWorld = worldConfig.武力等级 === '修仙';

if (isImmortalWorld && fandomEnabled) {
    // 修仙模式特殊处理
}
```

---

## 六、测试验证点

1. **新建游戏**: 选择不同武力等级，验证世界观生成内容差异
2. **存档兼容**: 旧存档（无武力等级字段）应默认使用 `中武`
3. **提示词注入**: 验证不同武力等级在系统提示词中的体现
4. **境界联动**: 修仙模式下境界体系是否正确生成
5. **UI 显示**: 新建游戏向导中武力等级选项是否正确显示
