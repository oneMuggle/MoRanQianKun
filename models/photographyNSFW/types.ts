/**
 * 写真约拍 NSFW 系统 - 类型定义
 * Phase 1 Step 1
 */

// 写真类型（9种）
export type 写真类型 = '商业写真' | '证件照' | '形象照' | '艺术照' | '私房照' | '情侣照' | '孕照' | '人体艺术' | '情趣写真';

// 拍摄场所（7种）
export type 拍摄场所 = '影棚' | '外景' | '酒店' | '民宿' | '个人住所' | '更衣室' | '野外';

// 拍摄风格（8种）
export type 拍摄风格 = '清新自然' | '时尚性感' | '复古情怀' | '个性潮流' | '私房暧昧' | '角色扮演' | '情色暗示' | '明确NSFW';

// 模特类型
export type 模特类型 = '专业模特' | '兼职模特' | '网红模特' | '素人模特' | '人体艺术模特' | '情趣模特';

// 模特职业状态
export type 模特职业状态 = '新人' | '发展期' | '成熟期' | '知名' | '过气' | '转型' | '退出';

// 模特保护意识
export type 模特保护意识 = '极度保护' | '适度保护' | '开放型' | '商业导向';

// 拍摄经历类型
export type 拍摄经历类型 = '正规拍摄' | '擦边拍摄' | '越界拍摄' | '偷拍受害者' | '主动泄露';

// 摄影师类型
export type 摄影师类型 = '商业摄影师' | '独立摄影师' | '网红摄影师' | '老法师' | '专业人像摄影师' | '私房摄影师' | '偷拍惯犯';

// 摄影师动机
export type 摄影师动机 = '纯艺术' | '商业盈利' | '泡妞' | '收藏' | '传播' | '偷拍';

// 摄影师信誉
export type 摄影师信誉 = '业界大佬' | '资深摄影师' | '普通摄影师' | '有争议' | '名声较差' | '惯犯';

// 服装类型
export type 服装类型 = '日常便装' | '泳装' | '内衣' | '睡衣' | '情趣内衣' | '透明薄纱' | '人体' | '角色服装' | '自定义服装';

// 拍摄尺度
export type 拍摄尺度 = 'G级' | 'PG-13' | 'R级' | 'NC-17' | 'XXX';

// 后期处理
export type 后期处理 = '纯自然' | '轻度美化' | '重度美颜' | '艺术处理' | 'AI换脸' | '合成处理';

// 越界行为类型
export type 越界行为类型 = '言语骚扰' | '要求加拍' | '服装要求' | '肢体接触' | '强迫拍摄' | '偷拍' | '威胁恐吓' | '下药';

// 泄露类型
export type 泄露类型 = '意外泄露' | '主动泄露' | '盗取传播' | '内部人员泄露' | '二手传播' | '钓鱼传播';

// 传播范围
export type 传播范围 = '未传播' | '小范围' | '论坛传播' | '社交媒体' | '图片网站' | '成人网站' | '全网扩散';

// 玩法层类型
export type 玩法层类型 = '经营管理' | '人际关系' | '灰色地带';

// NSFW内容强度
export type NSFW内容强度 = '微暗' | '暧昧' | '露骨';

// 写真玩法配置
export interface 写真玩法配置 {
  主要玩法层: 玩法层类型;
  次要玩法权重: number;        // 0-100, 次要层的出现频率
  启用道德选择: boolean;        // 灰色地带层的主动决策开关
  NSFW内容强度: NSFW内容强度;
}

/**
 * 模特核心状态
 * 模特档案、职业统计、安全记录、心理状态、经济状况
 */
export interface 模特核心状态 {
  // 基本信息
  id: string;
  姓名: string;
  类型: 模特类型;
  职业状态: 模特职业状态;
  保护意识: 模特保护意识;
  
  // 职业统计
  完成拍摄数: number;
  合作摄影师数: number;
  好评率: number;         // 0-100
  投诉次数: number;
  越界经历: 拍摄经历类型[];
  
  // 安全记录
  安全词使用次数: number;
  报警记录: number;
  黑名单摄影师: string[];
  
  // 心理状态
  心理阴影程度: number;   // 0-100
  自我认同: '受害者' | '中立' | '主动型';
  拍摄意愿: number;        // 0-100, 愿意接受拍摄的程度
  
  // 经济状况
  收入水平: number;        // 0-100
  主要收入来源: '正规拍摄' | '混合' | '边缘拍摄';
  
  // 关系网络
  信任摄影师: string[];
  优先合作摄影师: string[];
  
  // 附加数据
  创建时间: number;
  最后拍摄时间?: number;
}

/**
 * 摄影师核心状态
 * 摄影师档案、技能、口碑、隐藏属性（越界倾向、偷拍倾向、传播倾向）
 */
export interface 摄影师核心状态 {
  // 基本信息
  id: string;
  姓名: string;
  类型: 摄影师类型;
  信誉: 摄影师信誉;
  
  // 技能评分
  技术水平: number;        // 0-100
  艺术审美: number;       // 0-100
  沟通能力: number;       // 0-100
  
  // 口碑
  好评率: number;         // 0-100
  投诉次数: number;
  合作模特数: number;
  
  // 隐藏属性
  越界倾向: number;       // 0-100, 试探边界的倾向
  偷拍倾向: number;        // 0-100, 偷拍的可能性
  传播倾向: number;        // 0-100, 泄露照片的倾向
  
  // 动机
  主要动机: 摄影师动机;
  次要动机: 摄影师动机[];
  
  // 关系网络
  合作模特: string[];
  被拒绝次数: number;
  
  // 装备等级
  设备等级: '入门' | '进阶' | '专业' | '顶级';
  场地类型: '无' | '个人影棚' | '合作影棚' | '独立工作室';
  
  // 附加数据
  创建时间: number;
  最后拍摄时间?: number;
}

/**
 * 拍摄项目状态
 * 拍摄项目详情、约定vs实际、尺度递进追踪、泄露风险评分、违规记录
 */
export interface 拍摄项目状态 {
  // 项目标识
  id: string;
  模特ID: string;
  摄影师ID: string;
  
  // 约定信息
  约定类型: 写真类型;
  约定尺度: 拍摄尺度;
  约定场所: 拍摄场所;
  约定服装: 服装类型[];
  约定风格: 拍摄风格;
  
  // 实际执行
  实际尺度: 拍摄尺度;
  实际服装: 服装类型[];
  实际风格: 拍摄风格;
  
  // 进度追踪
  当前回合: number;
  总回合数: number;
  拍摄阶段: '准备' | '进行中' | '休息' | '换装' | '结束' | '交付';
  尺度递进次数: number;
  
  // 风险评估
  泄露风险值: number;      // 0-100
  违规记录: 越界行为类型[];
  
  // 交付信息
  交付状态: '待交付' | '分期交付' | '已完成' | '拒绝交付' | '拖延中';
  交付风险: '平台担保' | '直接交付' | '分期交付' | '拖延交付' | '拒绝交付';
  交付期限?: number;       // 回合数
  已交付照片数: number;
  总照片数: number;
  
  // 越界追踪
  是否越界: boolean;
  越界描述?: string;
  
  // BDSM联动（可选）
  涉及BDSM?: boolean;
  
  // 时间戳
  创建时间: number;
  更新时间: number;
  完成时间?: number;
}

/**
 * 泄露事件状态
 * 泄露事件、传播追踪、影响评估（心理、名誉、职业、生活）
 */
export interface 泄露事件状态 {
  // 事件标识
  id: string;
  拍摄项目ID: string;
  模特ID: string;
  摄影师ID: string;
  
  // 泄露详情
  泄露类型: 泄露类型;
  传播范围: 传播范围;
  发现时间: number;        // 回合数
  
  // 传播追踪
  当前传播范围: 传播范围;
  传播速度: '静止' | '缓慢' | '快速' | '爆发';
  传播平台: string[];
  
  // 影响评估
  模特心理影响: number;    // 0-100
  名誉损害度: number;      // 0-100
  职业影响度: number;       // 0-100
  生活影响度: number;       // 0-100
  
  // 应对状态
  是否已处理: boolean;
  处理方式?: '投诉' | '报警' | '委托律师' | '平台申诉' | '冷处理' | '公开发声' | '调查源头';
  处理结果?: string;
  
  // 时间戳
  发生时间: number;        // 回合数
  更新时间: number;
}
