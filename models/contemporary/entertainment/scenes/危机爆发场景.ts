/**
 * 危机爆发场景
 * 
 * 场景八：恋情曝光
 * 场景九：黑料曝光
 */

import type { 艺人核心状态 } from '../states/艺人状态';
import type { 艺人危机, 艺人危机类型, 危机处理方式 } from '../types';

/**
 * 危机爆发场景配置
 */
export interface 危机爆发场景配置 {
  危机类型: 艺人危机类型;
  触发条件: string;
  当前阶段: '苗头' | '发酵' | '爆发' | '热搜';
}

/**
 * 生成恋情曝光场景描述
 */
export function 生成恋情曝光场景描述(艺人: 艺人核心状态): string {
  let 描述 = `[微博热搜]\n\n`;
  描述 += `「${艺人.艺名}恋情曝光」引爆热搜\n\n`;
  描述 += `【曝光内容】\n`;
  描述 += `- 恋爱对象：未知（待设定）\n`;
  描述 += `- 曝光方式：狗仔偷拍/知情人爆料\n`;
  描述 += `- 证据：照片/视频\n\n`;
  描述 += `【粉丝反应】\n`;
  描述 += `- 脱粉回踩：表达愤怒、要求退圈\n`;
  描述 += `- 默默脱粉：悄悄离开\n`;
  描述 += `- 选择接受：继续支持\n`;
  描述 += `- 维护艺人：控评、反黑\n\n`;
  描述 += `【事业影响预估】\n`;
  描述 += `- 商务代言可能解约\n`;
  描述 += `- 影视资源可能受影响\n`;
  描述 += `- 粉丝结构可能改变\n`;

  return 描述;
}

/**
 * 生成黑料曝光场景描述
 */
export function 生成黑料曝光场景描述(艺人: 艺人核心状态): string {
  let 描述 = `[网络论坛/微博]\n\n`;
  描述 += `「818那个装纯的${艺人.艺名}」引发讨论\n\n`;
  描述 += `【黑料类型】\n`;
  描述 += `- 过去的不当言论？\n`;
  描述 += `- 早年的亲密照片？\n`;
  描述 += `- 疑似潜规则经历？\n`;
  描述 += `- 与工作人员的不正当关系？\n\n`;
  描述 += `【当前状态】\n`;
  描述 += `- 人设：${艺人.对外人设}\n`;
  描述 += `- 真实性格：${艺人.真实性格 || '未知'}\n`;
  描述 += `- 人设崩塌度：${艺人.人设崩塌度}%\n`;

  return 描述;
}

/**
 * 危机应对选项
 */
export function 获取危机应对选项(
  危机: 艺人危机
): { 选项: 危机处理方式; 描述: string; 成功率: number }[] {
  const 选项列表: { 选项: 危机处理方式; 描述: string; 成功率: number }[] = [
    {
      选项: '冷处理',
      描述: '不回应，等热度过去',
      成功率: 40,
    },
    {
      选项: '否认',
      描述: '坚决否认一切',
      成功率: 50,
    },
    {
      选项: '承认+道歉',
      描述: '承认事实并诚恳道歉',
      成功率: 70,
    },
    {
      选项: '澄清',
      描述: '提供证据澄清事实',
      成功率: 65,
    },
    {
      选项: '报警',
      描述: '针对恶意造谣报警',
      成功率: 60,
    },
    {
      选项: '发律师函',
      描述: '法律警告，起威慑作用',
      成功率: 55,
    },
  ];

  // 根据危机类型筛选可用选项
  if (危机.危机类型 === '恋情曝光') {
    选项列表.push({
      选项: '公关和解',
      描述: '与对方和解，快速平息风波',
      成功率: 75,
    });
  }

  if (危机.危机类型 === '粉丝连坐') {
    选项列表.push({
      选项: '弃车保帅',
      描述: '牺牲粉丝/员工保护自己',
      成功率: 80,
    });
  }

  return 选项列表;
}

/**
 * 危机演化描述
 */
export function 生成危机演化描述(危机: 艺人危机): string {
  const 阶段描述: Record<string, string> = {
    '苗头': '刚刚出现苗头，小范围讨论',
    '发酵': '小范围讨论开始扩散',
    '爆发': '大范围传播，登上热搜',
    '热搜': '持续占据热搜前列',
    '舆论反转': '出现反转，舆论开始改变',
    '平息': '逐渐平息，热度下降',
    '二次爆发': '旧事重提，再次引发关注',
  };

  let 描述 = `【危机演化】\n`;
  描述 += `当前阶段：${危机.当前阶段}\n`;
  描述 += `阶段描述：${阶段描述[危机.当前阶段]}\n`;
  描述 += `讨论量：${危机.讨论量.toLocaleString()}\n`;

  if (危机.热搜排名) {
    描述 += `热搜排名：第${危机.热搜排名}名\n`;
  }

  return 描述;
}

/**
 * 粉丝反应分化描述
 */
export function 生成粉丝反应分化描述(艺人: 艺人核心状态): string {
  const 总粉丝数 = 艺人.粉丝数量 || 1000000;
  const 脱粉回踩率 = 0.1;
  const 默默脱粉率 = 0.15;
  const 继续支持率 = 0.5;
  const 维护艺人率 = 0.25;

  let 描述 = `【粉丝反应分化】（基于${总粉丝数.toLocaleString()}粉丝）\n\n`;
  描述 += `- 脱粉回踩：约${Math.floor(totalFans * 脱粉回踩率).toLocaleString()}人\n`;
  描述 += `  表达愤怒、要求退圈、在网上攻击艺人\n\n`;
  描述 += `- 默默脱粉：约${Math.floor(totalFans * 默默脱粉率).toLocaleString()}人\n`;
  描述 += `  悄悄离开，不再关注\n\n`;
  描述 += `- 选择接受：约${Math.floor(totalFans * 继续支持率).toLocaleString()}人\n`;
  描述 += `  继续支持，认为艺人也有私生活\n\n`;
  描述 += `- 维护艺人：约${Math.floor(totalFans * 维护艺人率).toLocaleString()}人\n`;
  描述 += `  主动控评、反黑，为艺人辩护\n`;

  return 描述;
}
