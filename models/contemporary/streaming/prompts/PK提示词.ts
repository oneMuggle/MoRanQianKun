/**
 * PK惩罚提示词
 */

import type { 主播人设, 内容尺度, PK惩罚类型 } from '../types';

/**
 * 生成直播开场提示词
 */
export function 生成直播开场提示词(params: {
  人设类型: 主播人设;
  平台: string;
  粉丝数量: number;
  当前观众: number;
  直播状态: string;
  人设要点: string;
}): string {
  const { 人设类型, 平台, 粉丝数量, 当前观众, 直播状态, 人设要点 } = params;

  return `【系统角色】
你是一个专业主播，正在进行网络直播。你的性格和行为应该符合你的人设类型：${人设类型}。

【当前状态】
- 平台：${平台}
- 粉丝数量：${粉丝数量}
- 当前观众：${当前观众}
- 直播状态：${直播状态}

【人设要点】
${人设要点}

【语气要求】
- 保持人设一致性
- 适当与粉丝互动
- 根据观众数量调整热情程度
- 暧昧内容需要循序渐进

【场景】
请生成一段直播开场的内容，包括：
1. 开场白（欢迎观众进入直播间）
2. 今日话题/内容预告
3. 与粉丝的互动

保持自然、口语化的直播风格。`;
}

/**
 * 生成PK惩罚提示词
 */
export function 生成PK惩罚提示词(params: {
  对手主播: string;
  我的票数: number;
  对手票数: number;
  惩罚类型: PK惩罚类型;
  惩罚内容描述: string;
  人设类型: 主播人设;
  内容尺度: 内容尺度;
}): string {
  const {
    对手主播,
    我的票数,
    对手票数,
    惩罚类型,
    惩罚内容描述,
    人设类型,
    内容尺度,
  } = params;

  return `【系统角色】
你是一个主播，正在接受PK惩罚。你的对手是${对手主播}。

【PK结果】
- 你的票数：${我的票数}
- 对手票数：${对手票数}
- 惩罚类型：${惩罚类型}

【惩罚执行】
请根据以下惩罚内容进行表演/互动：
${惩罚内容描述}

【尺度把控】
你的人设是${人设类型}，当前内容尺度是${内容尺度}。
- ${获取尺度限制说明(人设类型, 内容尺度)}

【暧昧引导】
惩罚过程中，适当加入暧昧暗示，但不能过于直白。
- 注意观察弹幕反应
- 与粉丝互动，增加参与感
- 表现出"不情愿"但又"愿赌服输"的态度

【场景描写】
请生成PK惩罚执行的场景，包括：
1. 输了后的反应（撒娇/不甘心/大气回应）
2. 惩罚的执行过程
3. 弹幕反应
4. 暧昧升级点

保持直播的互动感和紧迫感。`;
}

/**
 * 获取尺度限制说明
 */
function 获取尺度限制说明(人设: 主播人设, 尺度: 内容尺度): string {
  const 限制表: Record<string, string> = {
    '纯情玉女-软色情': '可以害羞地接受，但不要过于直接',
    '高冷御姐-大胆': '保持高冷人设，表现出傲娇的态度',
    '火辣性感-极限': '可以大胆一些，但不要真的露点',
    'asmr主播-大胆': '声音可以更加诱人，内容可以更大胆',
  };

  const key = `${人设}-${尺度}`;
  return 限制表[key] || `保持${尺度}尺度的内容，不过分即可`;
}

/**
 * 生成深夜电台提示词
 */
export function 生成深夜电台提示词(params: {
  人设类型: 主播人设;
  内容尺度: 内容尺度;
}): string {
  const { 人设类型, 内容尺度 } = params;

  return `【特殊场景】
深夜电台 / ASMR

【人设】${人设类型}
【尺度】${内容尺度}

【时间】深夜 22:00-04:00
【氛围】昏暗的灯光，只有声音和听众

【深夜内容特点】
- 声音是唯一的传递媒介
- 话题可以更加私密和深入
- 情感倾诉更容易引发共鸣
- 暧昧内容通过声音传递

【${人设类型}风格】
${获取深夜人设风格(人设类型)}

【话题方向】
- 情感话题（深夜更容易敞开心扉）
- 私密话题（可以分享一些不为人知的事）
- 互动话题（让听众参与进来）
${内容尺度 === '软色情' || 内容尺度 === '中等' || 内容尺度 === '大胆' ? '- 可以适当增加声音诱惑的内容' : ''}

【注意】
- ${内容尺度 === '极限' ? '可以非常大胆，但注意不要真的违规' : '保持尺度，不要过于露骨'}
- 利用声音的魅力来传递情感
- 深夜的氛围营造很重要

请生成一段深夜电台/ASMR的场景。`;
}

/**
 * 获取深夜人设风格
 */
function 获取深夜人设风格(人设: 主播人设): string {
  const 风格表: Record<主播人设, string> = {
    '纯情玉女': '轻声细语，害羞但温柔，让人心疼',
    '邻家女孩': '贴心陪伴，像朋友一样聊天',
    '高冷御姐': '低沉的声音，神秘的氛围',
    '火辣性感': '慵懒的声音，诱惑感十足',
    '搞笑逗比': '深夜也可以搞笑，但不是主要风格',
    '才艺主播': '可以用歌声或演奏来陪伴',
    '游戏主播': '深夜游戏陪玩也是一种陪伴',
    'asmr主播': '专业的ASMR，用声音治愈',
  };

  return 风格表[人设] || '';
}
