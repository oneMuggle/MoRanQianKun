/**
 * 露出 NSFW 独立引擎 — 核心计算
 * 从 campusNSFW/exposureSystem.ts 迁移
 */

import type { 露出状态, 露出偏好等级, 旁观者, 旁观者反应, 紧张度状态, 网络流言状态, 紧张度阶段 } from '../../../models/exposureNSFW';
import type { 欲望阶段 } from '../../../models/nsfwCore';
import type { 露出个性系数 } from '../../../models/npcNSFWEnhancement';
import { 旁观者距离基础察觉率, 旁观者类型修正, 旁观者反应权重, 旁观者类型反应修正 } from '../../../models/exposureNSFW/constants';

export function 计算露出偏好推进(
  当前档案: { 露出状态?: 露出状态 },
  欲望阶段: 欲望阶段,
  选择倾向: string,
  当前暴露风险: number,
  npc性格外向: boolean,
  之前发生过暴露: boolean,
  个性系数?: 露出个性系数
): { 新等级: 露出偏好等级; 新进度: number; 升级: boolean } {
  const 露出状态 = 当前档案.露出状态;
  if (!露出状态) return { 新等级: 0, 新进度: 0, 升级: false };
  let 基础概率 = 0.05;
  if (欲望阶段 === '试探') 基础概率 = 0.05;
  else if (欲望阶段 === '渴望') 基础概率 = 0.15;
  else if (欲望阶段 === '沉沦') 基础概率 = 0.30;
  else if (欲望阶段 === '支配') 基础概率 = 0.50;
  let 修正系数 = 1.0;
  if (选择倾向 === '冒险暴露') 修正系数 *= 2.0;
  else if (选择倾向 === '安全隐蔽') 修正系数 *= 0.3;
  if (当前暴露风险 > 70) 修正系数 *= 1.5;
  if (npc性格外向) 修正系数 *= 1.3;
  else 修正系数 *= 0.7;
  if (之前发生过暴露) 修正系数 *= 1.8;
  // 个性系数修正
  if (个性系数) {
    修正系数 *= (1 + (个性系数.冒险倾向 - 50) / 100);
    修正系数 *= (1 - (个性系数.羞耻敏感度 - 50) / 200);
  }
  const 触发概率 = 基础概率 * 修正系数;
  if (Math.random() < 触发概率) {
    const 进度增加 = Math.floor(10 + Math.random() * 15);
    const 新进度 = Math.min(100, 露出状态.等级进度 + 进度增加);
    if (新进度 >= 100 && 露出状态.当前等级 < 5) {
      return { 新等级: (露出状态.当前等级 + 1) as 露出偏好等级, 新进度: 0, 升级: true };
    }
    return { 新等级: 露出状态.当前等级, 新进度, 升级: false };
  }
  return { 新等级: 露出状态.当前等级, 新进度: 露出状态.等级进度, 升级: false };
}

export function 计算紧张度(参数: {
  场景基础值: number; 周围人数: number; 互动强度系数: number;
  周围人状态: 紧张度状态['周围人状态'];
  npc公开行为: 紧张度状态['NPC公开行为'];
  露出偏好等级: 露出偏好等级;
  /** 服装暴露度 0-100，越高越容易引人注意 */
  服装暴露度?: number;
  /** 地点隐秘度 0-100，越高越私密 */
  地点隐秘度?: number;
  /** NPC冒险倾向 0-100 */
  npc冒险倾向?: number;
  /** 时间段: 白天/黄昏/夜晚/深夜 */
  时间段?: string;
  /** 环境噪音: 安静/正常/嘈杂 */
  声音环境?: string;
  /** 之前成功/失败次数 */
  过往经历?: { 成功次数: number; 失败次数: number };
}): { 紧张度: number; 阶段: 紧张度阶段 } {
  const { 场景基础值, 周围人数, 互动强度系数, npc公开行为, 露出偏好等级 } = 参数;
  let 人数修正 = 1.0;
  if (周围人数 <= 2) 人数修正 = 0.5;
  else if (周围人数 <= 10) 人数修正 = 1.0;
  else if (周围人数 <= 30) 人数修正 = 1.5;
  else 人数修正 = 2.0;
  let 行为修正 = 1.0;
  if (npc公开行为 === '正在发言') 行为修正 = 2.0;
  else if (npc公开行为 === '需要回应') 行为修正 = 1.5;

  // 基础紧张度
  let 紧张度 = 场景基础值 * 人数修正 * 互动强度系数 * 行为修正;

  // 服装暴露度修正：暴露度越高，紧张度越高
  if (参数.服装暴露度 !== undefined) {
    const 服装修正 = 1 + (参数.服装暴露度 / 100) * 0.5;
    紧张度 *= 服装修正;
  }

  // 地点隐秘度修正：隐秘度越低(越公开)，紧张度越高
  if (参数.地点隐秘度 !== undefined) {
    const 隐秘修正 = 1 + ((100 - 参数.地点隐秘度) / 100) * 0.4;
    紧张度 *= 隐秘修正;
  }

  // NPC性格修正：冒险倾向高降低紧张度
  if (参数.npc冒险倾向 !== undefined) {
    const 性格修正 = 1 - ((参数.npc冒险倾向 - 50) / 200);
    紧张度 *= 性格修正;
  }

  // 时间段修正：夜晚/深夜更紧张
  if (参数.时间段) {
    const 时间段映射: Record<string, number> = {
      '白天': 0.8, '黄昏': 1.0, '夜晚': 1.3, '深夜': 1.5,
    };
    紧张度 *= (时间段映射[参数.时间段] ?? 1.0);
  }

  // 声音环境修正：安静环境下更容易被发现
  if (参数.声音环境) {
    const 声音映射: Record<string, number> = {
      '安静': 1.3, '正常': 1.0, '嘈杂': 0.7,
    };
    紧张度 *= (声音映射[参数.声音环境] ?? 1.0);
  }

  // 过往经历修正：成功多降低紧张，失败多增加紧张
  if (参数.过往经历) {
    const { 成功次数, 失败次数 } = 参数.过往经历;
    const 经历修正 = 1 + (失败次数 * 0.1) - (成功次数 * 0.05);
    紧张度 *= 经历修正;
  }

  const 露出缓和 = 1.0 - (露出偏好等级 * 0.05);
  const 最终紧张度 = Math.min(100, Math.max(0, Math.round(紧张度 * 露出缓和)));

  // 判定阶段
  let 阶段: 紧张度阶段;
  if (最终紧张度 <= 20) 阶段 = '安全';
  else if (最终紧张度 <= 45) 阶段 = '微险';
  else if (最终紧张度 <= 70) 阶段 = '危险';
  else if (最终紧张度 <= 90) 阶段 = '极限';
  else 阶段 = '崩溃';

  return { 紧张度: 最终紧张度, 阶段 };
}

export function 判定旁观者察觉(旁观者: 旁观者, 紧张度: number): boolean {
  const 基础察觉 = 旁观者距离基础察觉率[旁观者.距离] ?? 5;
  const 类型修正 = 旁观者类型修正[旁观者.类型] ?? 1.0;
  const 紧张度修正 = 1.0 + (紧张度 / 100) * 0.5;
  return Math.random() * 100 < 基础察觉 * 类型修正 * 紧张度修正;
}

export function 模拟旁观者反应(已察觉: boolean, 旁观者?: 旁观者): 旁观者反应 {
  if (!已察觉) return '未察觉';
  const 基础权重 = [...旁观者反应权重];
  // 根据旁观者类型和关系亲密度修正权重
  if (旁观者) {
    const 类型修正 = 旁观者类型反应修正[旁观者.类型] ?? {};
    for (const [反应, 修正值] of Object.entries(类型修正)) {
      const 目标 = 基础权重.find(w => w.反应 === 反应);
      if (目标) {
        目标.权重 = Math.max(0, 目标.权重 + 修正值);
      }
    }
    // 关系亲密度修正：越亲密越倾向于保护（假装没看到）
    if (旁观者.关系亲密度 !== undefined) {
      const 亲密修正 = Math.round(旁观者.关系亲密度 * 0.2);
      const 保护项 = 基础权重.find(w => w.反应 === '假装没看到');
      if (保护项) {
        保护项.权重 = Math.max(0, 保护项.权重 + 亲密修正);
      }
      // 亲密度高时降低负面反应
      if (旁观者.关系亲密度 > 60) {
        const 负面反应: 旁观者反应[] = ['直接揭穿', '传播流言', '偷拍记录'];
        for (const 反应 of 负面反应) {
          const 项 = 基础权重.find(w => w.反应 === 反应);
          if (项) {
            项.权重 = Math.max(0, 项.权重 - Math.round(旁观者.关系亲密度! * 0.1));
          }
        }
      }
    }
  }
  const 总权重 = 基础权重.reduce((sum, r) => sum + r.权重, 0);
  let 随机值 = Math.random() * 总权重;
  for (const { 反应, 权重 } of 基础权重) {
    随机值 -= 权重;
    if (随机值 <= 0) return 反应;
  }
  return '假装没看到';
}

export function 模拟网络传播(
  当前网络流言: 网络流言状态 | undefined,
  有偷拍证据: boolean,
  口头流言等级: number,
  无新事件回合: number,
  正在辟谣: boolean
): 网络流言状态 {
  const 状态 = 当前网络流言 ?? {
    当前等级: 0, 传播渠道: [] as ('匿名论坛' | '校园群' | '截图流传' | '社交媒体')[], 有无证据: false,
    最新传播时间: new Date().toISOString(), 辟谣状态: '未辟谣' as const,
  };
  let 新等级 = 状态.当前等级;
  const 新渠道: ('匿名论坛' | '校园群' | '截图流传' | '社交媒体')[] = [...状态.传播渠道];
  let 有证据 = 状态.有无证据 || 有偷拍证据;
  if (有偷拍证据 && 口头流言等级 >= 2 && 新等级 < 1) { 新等级 = 1; if (!新渠道.includes('匿名论坛')) 新渠道.push('匿名论坛'); }
  if (口头流言等级 >= 3 && 新等级 < 2) { 新等级 = 2; if (!新渠道.includes('校园群')) 新渠道.push('校园群'); }
  if (有证据 && 口头流言等级 >= 4 && 新等级 < 3) { 新等级 = 3; if (!新渠道.includes('截图流传')) 新渠道.push('截图流传'); }
  if (口头流言等级 >= 5 && 新等级 < 4) { 新等级 = 4; if (!新渠道.includes('社交媒体')) 新渠道.push('社交媒体'); }
  if (无新事件回合 >= 15) {
    const 衰减值 = 正在辟谣 ? Math.floor(无新事件回合 / 7.5) : Math.floor(无新事件回合 / 15);
    新等级 = Math.max(0, 新等级 - 衰减值);
  }
  return { ...状态, 当前等级: 新等级, 传播渠道: 新渠道, 有无证据: 有证据, 最新传播时间: new Date().toISOString(), 辟谣状态: 正在辟谣 ? '正在辟谣' : 状态.辟谣状态 };
}

export function 计算回合衰减(参数: { 当前暴露风险: number; 当前流言等级: number; 正在辟谣?: boolean }): { 新暴露风险: number; 新流言等级: number } {
  const { 当前暴露风险, 当前流言等级, 正在辟谣 } = 参数;
  return { 新暴露风险: Math.max(0, 当前暴露风险 - 5), 新流言等级: Math.max(0, 当前流言等级 - (正在辟谣 ? 4 : 2)) };
}

export function 计算露出衰减(参数: { 当前露出状态: 露出状态 | undefined; 无互动回合: number }): 露出状态 | undefined {
  const { 当前露出状态, 无互动回合 } = 参数;
  if (!当前露出状态 || 当前露出状态.当前等级 === 0) return 当前露出状态;
  let 新进度 = Math.max(0, 当前露出状态.等级进度 - Math.floor(无互动回合 / 3) * 10);
  const 旧等级 = 当前露出状态.当前等级;
  let 新等级: 露出偏好等级 = 旧等级;
  if (新进度 === 0 && 新等级 > 0 && 无互动回合 >= 10) { 新等级 = Math.max(0, 新等级 - 1) as 露出偏好等级; 新进度 = 50; }
  return { ...当前露出状态, 当前等级: 新等级 as 露出偏好等级, 等级进度: 新进度 };
}

/** 紧张度阶段展示信息 */
const 紧张度阶段展示: Record<紧张度阶段, { 颜色: string; 图标: string; 描述: string }> = {
  '安全': { 颜色: '#22c55e', 图标: '🛡️', 描述: '当前环境安全，可正常互动' },
  '微险': { 颜色: '#eab308', 图标: '⚠️', 描述: '稍有风险，注意周围环境' },
  '危险': { 颜色: '#f97316', 图标: '🔥', 描述: '紧张度较高，随时可能被发现' },
  '极限': { 颜色: '#ef4444', 图标: '💥', 描述: '极度紧张，暴露风险极高' },
  '崩溃': { 颜色: '#dc2626', 图标: '🚨', 描述: '紧张度爆表，即将或已经暴露' },
};

export function 获取紧张度阶段展示(阶段: 紧张度阶段) {
  return 紧张度阶段展示[阶段];
}
