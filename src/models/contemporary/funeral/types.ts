/**
 * 丧葬/殡葬行业 NSFW 模块 - 类型定义
 * 
 * v1.0: 核心类型系统、状态定义
 * 现实参考：殡葬师、墓地销售、遗物整理、丧葬策划
 */

// ==================== 殡葬职业类型 ====================

export type 殡葬职业类型 =
  | '殡葬师'             // 殡葬师
  | '墓地销售'           // 墓地销售
  | '遗物整理师'         // 整理遗物
  | '丧葬策划师'         // 策划葬礼
  | '守墓人'             // 守墓人
  | '入殓师';            // 入殓

export type 殡葬服务类型 =
  | '遗体处理'           // 遗体处理
  | '火化安排'           // 火化
  | '墓地销售'           // 墓地
  | '葬礼策划'           // 策划
  | '遗物整理'           // 遗物
  | '心理安抚'           // 心理安抚
  | '守灵服务';          // 守灵

export type 殡葬客户类型 =
  | '普通家庭'           // 普通
  | '富豪家族'           // 有钱人
  | '孤独死者'           // 无人认领
  | '意外死亡'           // 意外
  | '特殊宗教';          // 宗教

// ==================== 场景类型 ====================

export type 殡葬场景 =
  | '殡仪馆'              // 殡仪馆
  | '墓地'                // 墓地
  | '太平间'              // 医院太平间
  | '客户家中'            // 上门服务
  | '葬礼现场'            // 葬礼
  | '告别仪式';           // 告别

// ==================== 暧昧类型 ====================

export type 殡葬暧昧类型 =
  | '遗体处理的亲密'      // 处理遗体时的接触
  | '孤独死的清理'        // 孤独死后的清理
  | '遗物中的秘密'        // 发现死者秘密
  | '脆弱时期的信任'      // 家属脆弱时
  | '死亡与欲望'          // 殡葬人员的特殊心理
  | '墓地幽会';           // 在墓地的特殊幽会

// ==================== 核心状态接口 ====================

// 殡葬从业者状态
export interface 殡葬从业者状态 {
  ID: string;
  化名: string;
  本名?: string;
  性别: '男' | '女';
  年龄: number;

  // 职业
  殡葬职业: 殡葬职业类型;
  从业年限: number;

  // 工作
  处理遗体数: number;              // 处理过的遗体数
  客户信任度: number;              // 0-100
  发现秘密数量: number;            // 发现死者秘密数
  孤独死处理数: number;           // 处理的孤独死

  // 收入
  月收入: number;
  灰色收入: number;                // 灰色收入

  // 工作
  工作满意度: number;               // 0-100
  职业倦怠: number;                // 0-100
  心理阴影: number;                 // 0-100

  // 心理
  职业认同: number;                 // 0-100
  死亡认知: number;                // 0-100，对死亡的态度
  欲望指数: number;                 // 0-100，特殊的欲望
  羞耻度: number;                   // 0-100

  // 安全
  法律风险: number;                  // 0-100
  心理疾病风险: number;              // 0-100
}

// 死者/家属状态
export interface 殡葬客户状态 {
  ID: string;
  死者名: string;
  性别: '男' | '女';
  年龄: number;

  // 死者
  死亡原因: string;
  死亡类型: '正常死亡' | '意外死亡' | '自杀' | '他杀' | '孤独死';
  有无家属: boolean;
  家属关系?: string;

  // 遗产
  遗产状况: number;                // 遗产金额
  遗产纠纷: boolean;               // 是否有纠纷

  // 遗物
  遗物数量: number;
  遗物中有无敏感物品: boolean;      // 有无敏感物品
  敏感物品描述?: string;

  // 殡葬
  选择的套餐: string;
  花费金额: number;
}
