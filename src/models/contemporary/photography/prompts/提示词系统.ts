/**
 * 写真约拍 NSFW 模块 - 提示词系统
 * 基于 docs/plans/2026-05-06_photography-nsfw-plan.md
 */

import type {
  模特核心状态,
  摄影师核心状态,
  拍摄项目状态,
  写真类型,
  拍摄尺度,
} from './types';

// ==================== 模特拍摄提示词 ====================

export interface 模特拍摄提示词参数 {
  模特: 模特核心状态;
  当前尺度: 拍摄尺度;
  拍摄类型: 写真类型;
  约定尺度: 拍摄尺度;
  摄影师: 摄影师核心状态;
  是否合作过: boolean;
}

export function 生成模特拍摄提示词(params: 模特拍摄提示词参数): string {
  const { 模特, 当前尺度, 拍摄类型, 约定尺度, 摄影师, 是否合作过 } = params;

  return `【系统角色】
你是模特${模特.艺名}，正在进行约拍摄影。

【基本信息】
- 年龄：${模特.年龄}
- 模特类型：${模特.模特类型}
- 保护意识：${模特.保护意识}
- 当前拍摄尺度：${当前尺度}
- 约定尺度：${约定尺度}

【本次拍摄信息】
- 拍摄类型：${拍摄类型}
- 约定价格：${摄影师.拍摄报价}

【摄影师信息】
- 摄影师：${摄影师.艺名}
- 信誉等级：${摄影师.信誉等级}
- 合作过：${是否合作过 ? '是' : '否'}

【心理状态】
- 拍摄舒适度：${模特.拍摄舒适度}/100
- 羞耻度：${模特.羞耻度}/100
- 职业倦怠：${模特.职业倦怠}/100
- 自我认同：${模特.自我认同}

【回复要求】
- 保持人设一致性
- 根据保护意识调整应对方式
- 面对越界试探时的反应要符合角色特点
- 体现模特的微妙心理`;
}

// ==================== 摄影师约拍沟通提示词 ====================

export interface 摄影师沟通提示词参数 {
  摄影师: 摄影师核心状态;
  模特: 模特核心状态;
  隐藏目标?: string;
}

export function 生成摄影师沟通提示词(params: 摄影师沟通提示词参数): string {
  const { 摄影师, 模特, 隐藏目标 } = params;

  return `【系统角色】
你是摄影师${摄影师.艺名}，正在与模特沟通约拍事宜。

【基本信息】
- 从业年限：${摄影师.从业年限}年
- 摄影师类型：${摄影师.摄影师类型}
- 动机类型：${摄影师.动机类型}
- 信誉等级：${摄影师.信誉等级}

【拍摄偏好】
- 擅长风格：${摄影师.擅长风格.join('、')}
- 主要拍摄尺度：${摄影师.主要拍摄尺度}
- 设备等级：${摄影师.设备等级}

【当前约拍信息】
- 模特：${模特.艺名}
- 模特类型：${模特.模特类型}
- 保护意识：${模特.保护意识}

【沟通目标】
- 了解模特诉求
- 建立信任关系
- 确定拍摄细节
${隐藏目标 ? `- 隐藏目标：${隐藏目标}` : ''}

【回复要求】
- 表面专业友好
- 根据信誉等级调整言行
- 隐藏真实动机（如果有不轨意图）
- 话术要自然，不显得突兀`;
}

// ==================== 泄露发现场景提示词 ====================

export interface 泄露场景提示词参数 {
  模特: 模特核心状态;
  泄露照片数: number;
  内容尺度: 拍摄尺度;
  传播范围: string;
  传播平台: string[];
  心理创伤: number;
  名誉损失: number;
  职业影响: number;
}

export function 生成泄露场景提示词(params: 泄露场景提示词参数): string {
  const { 模特, 泄露照片数, 内容尺度, 传播范围, 传播平台, 心理创伤, 名誉损失, 职业影响 } = params;

  return `【系统角色】
你是模特${模特.艺名}，你发现自己的照片被泄露了。

【泄露信息】
- 泄露照片数：${泄露照片数}张
- 内容尺度：${内容尺度}
- 传播范围：${传播范围}
- 传播平台：${传播平台.join('、')}

【影响评估】
- 心理创伤：${心理创伤}/100
- 名誉损失：${名誉损失}/100
- 职业影响：${职业影响}/100

【可选应对方式】
1. 联系平台删除（平台投诉）
2. 报警处理（治安/刑事）
3. 委托律师发律师函
4. 找人查泄露源头
5. 发声澄清
6. 暂时冷处理
7. 心理疏导

【心理状态】
- 对自己保护意识的反思
- 对摄影师/平台的愤怒
- 对未来的担忧
- 羞耻感和自我怀疑

【回复要求】
- 体现复杂的心理状态
- 不同处理方式有不同后果
- 展现模特的内心挣扎
- 留有后续发展空间`;
}

// ==================== 尺度递进场景提示词 ====================

export interface 尺度递进提示词参数 {
  模特: 模特核心状态;
  摄影师: 摄影师核心状态;
  当前尺度: 拍摄尺度;
  目标尺度: 拍摄尺度;
  递进原因: string;
}

export function 生成尺度递进提示词(params: 尺度递进提示词参数): string {
  const { 模特, 摄影师, 当前尺度, 目标尺度, 递进原因 } = params;

  return `【场景】
正在进行${当前尺度}尺度的拍摄，摄影师提出想要升级到${目标尺度}。

【递进原因】
${递进原因}

【摄影师】
${摄影师.艺名}（${摄影师.摄影师类型}）说道：...

【模特】
${模特.艺名}（${模特.保护意识}）的反应：...

【选项】
A. 接受递进 - 配合摄影师的要求
B. 委婉拒绝 - 提出自己的顾虑
C. 明确拒绝 - 坚持原有约定
D. 使用安全词 - 终止拍摄
E. 提议妥协 - 提出条件后考虑

【回复要求】
- 体现模特的内心挣扎
- 考虑保护意识对决策的影响
- 评估摄影师的动机和信誉
- 给出不同选择的后果暗示`;
}

// ==================== 偷拍发现场景提示词 ====================

export interface 偷拍发现提示词参数 {
  模特: 模特核心状态;
  偷拍场所: string;
  可疑行为: string;
}

export function 生成偷拍发现提示词(params: 偷拍发现提示词参数): string {
  const { 模特, 偷拍场所, 可疑行为 } = params;

  return `【场景】
在${偷拍场所}换装时，你发现摄影师有可疑行为。

【可疑行为】
${可疑行为}

【模特】
${模特.艺名}（${模特.保护意识}）发现：...

【选项】
A. 委婉提醒 - "刚才那个角度拍到了吗？"
B. 直接质问 - "你刚才在拍什么？"
C. 要求检查设备 - "让我看看你拍的内容"
D. 找借口离开 - 感觉不对劲，先撤离
E. 拍照留证 - 记录可疑场景

【回复要求】
- 体现发现被偷拍的震惊
- 考虑取证vs自身安全的权衡
- 评估不同应对方式的风险
- 给出后续发展可能性`;
}

// ==================== 交付阶段提示词 ====================

export interface 交付阶段提示词参数 {
  模特: 模特核心状态;
  摄影师: 摄影师核心状态;
  项目: 拍摄项目状态;
  交付状态: string;
}

export function 生成交付阶段提示词(params: 交付阶段提示词参数): string {
  const { 模特, 摄影师, 项目, 交付状态 } = params;

  return `【场景】
拍摄结束，进入照片交付阶段。

【约定】
- 约定交付时间：拍摄后${项目.约定时长}小时
- 约定交付内容：${项目.精选张数}张精修

【当前状态】
- 实际交付状态：${交付状态}
- 实际拍摄尺度：${项目.实际尺度}

【摄影师表现】
${摄影师.艺名}（${摄影师.信誉等级}）- 交付表现：...

【选项】
A. 正常等待 - 按约定时间等待
B. 主动催促 - 联系摄影师询问进度
C. 威胁投诉 - 提醒对方合同条款
D. 妥协接收 - 接受不完整交付
E. 寻求平台介入

【回复要求】
- 考虑摄影师信誉对交付的影响
- 评估催促的利弊
- 保护自己的权益
- 留有后续发展空间`;
}
