/**
 * NSFW 共享核心类型
 * 欲望状态机、关系轨道、后果系统 — 被校园/BDSM/露出系统共同使用
 */

// 欲望状态机
export type 欲望阶段 = '克制' | '试探' | '渴望' | '沉沦' | '支配';
export type 关系轨道 = '纯爱' | '暧昧' | '肉体' | '支配' | '多角';

export type 权力倾向 = '绝对支配' | '偏支配' | '切换者' | '偏服从' | '绝对服从';

export type 后果类型 =
  | '暴露风险' | '流言传播' | '学业下滑'
  | '多角冲突' | '心理负担' | '毕业危机'
  | '网络传播' | '偷拍泄露' | '网络发酵'
  | '校园调查' | '关系强制公开' | '名誉损毁'
  | '社交孤立' | '社团除名' | '处分警告'
  | '指令暴露' | '束缚痕迹' | '称呼异常'
  | '信物被发现' | '契约泄露' | '服从过度'
  | '权力失衡' | '心理依赖';

export interface 后果记录 {
  id: string;
  触发时间: string;
  类型: 后果类型;
  关联NPC?: string;
  严重程度: '轻微' | '中等' | '严重' | '毁灭';
  描述: string;
  持续回合: number;
  是否已解决: boolean;
}
