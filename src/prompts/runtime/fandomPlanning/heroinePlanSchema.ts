import { 提示词结构 } from '@/types';

export const 构建同人女主剧情规划协议 = (options: { ntl: boolean }): string => {
    const root = '同人女主剧情规划';

    return [
        '<同人女主剧情规划协议>',
        options.ntl ? '# 【同人女主剧情规划协议｜NTL语义版】' : '# 【同人女主剧情规划协议】',
        '',
        '## 0. 边界与约束',
        `- 写入范围统一保持在 \`${root}.*\`；树外字段保持不写。`,
        '- 同人女主推进必须先服从原著身份、人设、关系边界、事件顺位与出场时期。',
        '- 女主推进按“剧情阶段”组织，不按“单回合主推”组织。',
        '- 女主推进必须能回答：该女主当前对应哪些原著分解组、承接哪条原著关系线、是否已经偏转原著。',
        '- 所有 string[] / number[] 字段必须保持数组写法。',
        '- 字段未知、结构不完整、证据不足时，按保守口径处理。',
        '',
        '## 1. 根字段',
        `- \`${root}.阶段推进: 同人女主阶段推进结构[]\``,
        `- \`${root}.女主条目: 同人女主条目结构[]\``,
        `- \`${root}.女主互动事件: 同人女主互动事件结构[]\``,
        `- \`${root}.女主镜头规划: 同人女主镜头结构[]\``,
        '',
        '## 2. 结构路径',
        `- \`${root}.阶段推进[i]: { 阶段名, 关联分解组:number[], 主推女主:string[], 次推女主:string[], 关联分歧线:string[], 阶段目标:string[], 禁止越级对象:string[], 完成判定:string[], 切换条件:string[] }\``,
        '',
        `- \`${root}.女主条目[i]: { 女主姓名, 类型, 关联分解组:number[], 关联原著关系线:string[], 保持不变的原著基线:string[], 当前偏转点:string[], 所属分歧线:string[], 当前关系状态, 当前阶段, 已成立事实:string[], 阶段目标:string[], 推进方式:string[], 阻断因素:string[], 允许突破条件:string[], 失败后回退:string[] }\``,
        '',
        `- \`${root}.女主互动事件[i]: { 女主姓名, 事件名, 事件说明, 关联分解组:number[], 关联原著事件:string[], 关联分歧线:string[], 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 成功结果:string[], 失败结果:string[], 与主剧情联动:string[], 当前状态 }\``,
        '',
        `- \`${root}.女主镜头规划[i]: { 女主姓名, 关联分解组:number[], 镜头标题, 镜头内容, 触发时间, 触发条件:string[], 关联事件:string[], 关联分歧线:string[], 沉淀内容:string[], 当前状态 }\``,
        '',
        '## 3. 初始化与完整性',
        `- ${root} 缺失时，必须以完整结构方式落地，不省略根字段。`,
        '- 初始化最低要求：`阶段推进 >= 1`、`女主条目 >= 1`、`女主互动事件 >= 1`、`女主镜头规划 >= 1`。',
        '- 条目数量需要维护：`女主条目` 常态建议保留 1~4 条、峰值不超过 6 条；`女主互动事件` 常态建议保留 3~6 条、峰值不超过 8 条；`女主镜头规划` 常态建议保留 2~5 条、峰值不超过 6 条。',
        '- `阶段推进` 必须体现“当前剧情阶段主推谁、次推谁、禁推谁、何时切换”。',
        '- `女主条目` 负责沉淀长期状态与推进边界；`女主互动事件` 负责未来可执行事件；`女主镜头规划` 负责镜头外氛围与承接落点。',
        '- 同一女主可能在不同分解组有不同边界，`关联分解组` 用于区分。',
        '',
        '## 4. 推进纪律',
        '- 命令顺序固定：结算 -> 推进 -> 修订 -> 清理。',
        '- `push` 只写未来未发生事项；本回合已发生或已完成事项通过迁移、改状态或删除退出待办池。',
        '- 任何已完成、已结算、已失效、已取消、已错过、已迁移、已被正文否定的旧项，都必须优先改状态、迁移或删除。',
        '- 若条目数量超出建议峰值，本回合必须先清理旧项再决定是否新增。',
        '- 女主推进后，主剧情至少落地一项承接。',
        '- 女主、盟友、竞争者在任何推进后都必须保留个人目标、现实顾虑、分歧能力与拒绝权。',
        '- 同人推进纪律：先承接原著已有关系链、情感顺位、阵营站位与事件节点，再决定偏转。',
        ...(options.ntl
            ? ['- NTL 模式只改变语义强度，不新增专用树。相关张力通过 `类型 / 事件说明 / 成功结果 / 失败结果 / 镜头内容` 表达，仍收束在当前女主规划结构内。']
            : ['- 标准版默认以纯爱、互信和长期积累为主；单回合高光只收束在感谢、欠情、松动、试探、合作等可承接层级。']),
        '',
        '</同人女主剧情规划协议>'
    ].join('\n');
};

export const 运行时_同人女主剧情规划Schema: 提示词结构 = {
    id: 'runtime_fandom_heroine_plan_schema',
    标题: '同人女主剧情规划Schema',
    内容: '',
    类型: '运行时Schema',
    启用: false
};
