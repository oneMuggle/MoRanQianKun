/**
 * 世界数据结构 Schema 提示词
 *
 * 本文件定义动态世界变量结构，作为世界演变引擎的参考。
 *
 * 核心设计原则（来自「动态世界联动重构计划」）：
 * 1. 世界演变必须先回答"什么时候可以执行"，再回答"执行后发生了什么"
 * 2. 因此要拆成：待执行事件 / 进行中事件 / 已结算事件
 * 3. NPC 也要有自己的行动时间门槛与条件门槛
 * 4. 不需要 `势力常识` 和 `地理常识`（属于世界观提示词职责）
 */

export const 世界变量结构参考提示词 = `
<世界变量结构参考>
# 世界独立数据结构参考

## 0. 使用方式
- 当前世界演变只认这些根路径：\`世界\` 与必要时联动的 \`环境\`。
- 路径使用无前缀写法；命令统一从 \`世界\` 顶层起写。
- 这里只定义世界层字段树与元素结构，不负责命令顺序与输出格式。
- 运行时注入给模型时，数组会保留原形；若数组元素是对象，阅读视图中通常会额外带一行 \`"[0]"\` / \`"[1]"\` 这类锚点，仅用于阅读定位。
- 更新已有对象时，只有在目标对象已存在且结构完整时，才使用 \`set 世界.活跃NPC列表[i].字段 字段值\` 精准刷新。
- 新增数组项时，优先使用 \`push 世界.xxx = {...}\`，并一次写完整对象。
- 若 \`世界.xxx[i]\` 尚不存在，不要直接 \`set 世界.xxx[i].字段\`；先 \`push 世界.xxx = {完整对象}\` 建项，再做后续局部修改。
- 若需要联动环境时间，只写 \`set 环境.时间 = "YYYY:MM:DD:HH:MM"\`，不要拆开写年月日时分。
- \`沉淀内容[] / 归档内容[]\` 这类累计数组，若只是新增单条事实，优先用 \`push\` 追加，不要整组 \`set\` 覆盖。
- \`世界.进行中事件[i].当前进展\` 是单个"当前摘要"字符串，不是历史日志数组；每轮若有变化，应综合旧进展后用 \`set\` 覆写成最新摘要。
- 字段暂缺时，可保留空字符串、空数组或 null；但与时间、条件、状态直接相关的核心字段不要留空。
- 命令保持最小路径：只改哪个字段，就写哪个字段。

## 1. 必须删除的旧字段（不属于运行时动态状态）
- \`势力常识\` - 属于世界观提示词职责
- \`地理常识\` - 属于世界观提示词职责

## 2. 世界演变可写根树
世界
├─ 活跃NPC列表
├─ 待执行事件
├─ 进行中事件
├─ 已结算事件
├─ 世界镜头规划
├─ 江湖史册
├─ 地图
└─ 建筑

## 3. 世界
世界
├─ 活跃NPC列表: Array<世界.活跃NPC列表[i]>
├─ 待执行事件: Array<世界.待执行事件[i]>
├─ 进行中事件: Array<世界.进行中事件[i]>
├─ 已结算事件: Array<世界.已结算事件[i]>
├─ 世界镜头规划: Array<世界.世界镜头规划[i]>
├─ 江湖史册: Array<世界.江湖史册[i]>
├─ 地图: Array<世界.地图[i]>
└─ 建筑: Array<世界.建筑[i]>

### 3.1 【类型别名】世界.活跃NPC列表[i]
世界.活跃NPC列表[i] = {
  姓名:string,
  所属势力:string,
  当前位置:string,
  当前状态:string,
  当前行动:string,
  最早行动时间:string(YYYY:MM:DD:HH:MM),
  最晚行动时间:string(YYYY:MM:DD:HH:MM),
  前置条件:string[],
  触发条件:string[],
  阻断条件:string[],
  行动完成判定:string[],
  行动完成后影响:string[],
  关联分解组:number[],
  关联分歧线:string[]
}

### 3.2 【类型别名】世界.待执行事件[i]
世界.待执行事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  计划执行时间:string(YYYY:MM:DD:HH:MM),
  最早执行时间:string(YYYY:MM:DD:HH:MM),
  最晚执行时间:string(YYYY:MM:DD:HH:MM),
  前置条件:string[],
  触发条件:string[],
  阻断条件:string[],
  执行后影响:string[],
  错过后影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[],
  当前状态:string
}

### 3.3 【类型别名】世界.进行中事件[i]
世界.进行中事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  开始时间:string(YYYY:MM:DD:HH:MM),
  预计结束时间:string(YYYY:MM:DD:HH:MM),
  当前进展:string,
  已产生影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[]
}

### 3.4 【类型别名】世界.已结算事件[i]
世界.已结算事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  结算时间:string(YYYY:MM:DD:HH:MM),
  事件结果:string[],
  长期影响:string[],
  是否进入史册:boolean,
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[]
}

### 3.5 【类型别名】世界.世界镜头规划[i]
世界.世界镜头规划[i] = {
  镜头标题:string,
  镜头内容:string,
  触发时间:string(YYYY:MM:DD:HH:MM),
  触发条件:string[],
  关联人物:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[],
  沉淀内容:string[],
  当前状态:string
}

### 3.6 【类型别名】世界.江湖史册条目[i]
世界.江湖史册[i] = {
  标题:string,
  归档时间:string(YYYY:MM:DD:HH:MM),
  归档内容:string[],
  长期影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分歧线:string[]
}

### 3.7 【类型别名】世界.地图[i]
世界.地图[i] = {
  名称:string,
  坐标:string,
  描述:string,
  归属:{
    大地点:string,
    中地点:string,
    小地点:string
  },
  内部建筑:string[]
}

### 3.8 【类型别名】世界.建筑[i]
世界.建筑[i] = {
  名称:string,
  描述:string,
  归属:{
    大地点:string,
    中地点:string,
    小地点:string
  }
}

## 4. NPC 行动时间门槛说明
- \`最早行动时间\`：该 NPC 最早可以开始行动的时间
- \`最晚行动时间\`：该 NPC 最晚必须开始行动的时间
- \`前置条件\`：NPC 行动前必须满足的条件
- \`触发条件\`：NPC 行动的具体触发条件
- \`阻断条件\`：阻止 NPC 行动的条件
- \`行动完成判定\`：判断 NPC 行动是否完成的标准

## 5. 世界事件三层结构
- \`待执行事件\`：尚未开始但已具备时间门槛与条件门槛
- \`进行中事件\`：已经开始、正在发酵
- \`已结算事件\`：结果已落定

## 6. 时间门槛判定规则
所有世界事件都按以下规则使用：
1. 当前时间早于 \`最早执行时间\`，则该事件不能触发
2. 当前时间达到 \`最早执行时间\` 但 \`前置条件\` 未满足，则该事件不能触发
3. \`前置条件\` 满足但 \`触发条件\` 未满足，则该事件处于待触发
4. 若命中任一 \`阻断条件\`，则该事件不能按原路径触发
5. 当前时间超过 \`最晚执行时间\` 且事件仍未发生，则该事件进入"错过 / 偏转"判断

## 7. 同人模式扩展字段
- \`关联分解组:number[]\`：该 NPC/事件关联的原著分解组
- \`关联分歧线:string[]\`：该 NPC/事件关联的分歧线

</世界变量结构参考>
`;
