/**
 * 剧情规划 Schema 提示词
 *
 * 来自「剧情规划 / 女主规划 / 小说分解 / 动态世界联动重构计划」
 *
 * 核心设计原则：
 * 1. 不再以"长期/中期/短期"层级为主，而以执行区块为主
 * 2. 所有未来项都必须能回答六个问题：
 *    - 这件事是什么
 *    - 最早什么时候能发生
 *    - 还需要什么前置条件
 *    - 什么条件满足后立即可触发
 *    - 什么情况下不能触发或需要延后
 *    - 触发后会沉淀什么结果
 * 3. 原创剧情规划不包含 `关联分解组`、`原著事件`、`分歧线` 等同人专用锚点
 * 4. 不使用 V2 之类版本号命名
 */

import { 提示词结构 } from '@/types';

export const 构建原创剧情规划协议 = (): string => [
    '<剧情规划协议>',
    '# 【剧情规划协议】',
    '',
    '## 0. 边界与约束',
    '- 写入范围统一保持在 `剧情规划.*`；树外字段保持不写。',
    '- 原创剧情规划不包含 `关联分解组`、`原著事件`、`分歧线` 等同人专用锚点。',
    '- 女主推进按"剧情阶段"组织，不按"单回合主推"组织。',
    '- 所有 string[] / number[] 字段必须保持数组写法。',
    '- 字段未知、结构不完整、证据不足时，按保守口径处理。',
    '',
    '## 1. 根字段',
    '- `剧情规划.当前章目标: string[]`',
    '- `剧情规划.当前章任务: 剧情任务结构[]`',
    '- `剧情规划.跨章延续事项: 剧情延续事项结构[]`',
    '- `剧情规划.待触发事件: 剧情待触发事件结构[]`',
    '- `剧情规划.镜头规划: 剧情镜头结构[]`',
    '- `剧情规划.换章规则: 剧情换章规则结构`',
    '',
    '## 2. 结构路径',
    '`剧情规划.当前章任务[i]: { 标题, 任务说明, 计划执行时间, 最早执行时间, 最晚执行时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 执行动作:string[], 完成判定:string[], 失败后转移:string[], 完成后沉淀:string[], 关联人物:string[], 关联地点:string[], 关联势力:string[], 当前状态 }`',
    '',
    '`剧情规划.跨章延续事项[i]: { 标题, 延续原因:string[], 当前状态:string[], 延续到何时, 后续接续条件:string[], 终止条件:string[] }`',
    '',
    '`剧情规划.待触发事件[i]: { 事件名, 事件说明, 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 成功结果:string[], 失败结果:string[], 当前状态 }`',
    '',
    '`剧情规划.镜头规划[i]: { 镜头标题, 镜头内容, 触发时间, 前置条件:string[], 触发条件:string[], 阻断条件:string[], 关联任务:string[], 关联人物:string[], 关联地点:string[], 沉淀内容:string[], 当前状态 }`',
    '',
    '`剧情规划.换章规则: { 本章完成判定:string[], 允许切章条件:string[], 禁止切章条件:string[], 切章后需沉淀内容:string[], 切章后需清空字段:string[], 切章后需重建字段:string[] }`',
    '',
    '## 3. 执行约束（必须回答的六个问题）',
    '每个任务或事件都必须能回答以下问题：',
    '1. 这件事是什么',
    '2. 最早什么时候能发生',
    '3. 还需要什么前置条件',
    '4. 什么条件满足后立即可触发',
    '5. 什么情况下不能触发或需要延后',
    '6. 触发后会沉淀什么结果',
    '',
    '## 4. 换章规则',
    '`换章规则` 必须包含：',
    '- `本章完成判定`：可验证的完成标准，不是概括性描述',
    '- `允许切章条件`：满足这些条件时可以切章',
    '- `禁止切章条件`：满足这些条件时禁止切章',
    '- `切章后需沉淀内容`：切章后需要沉淀到历史卷宗的内容',
    '- `切章后需清空字段`：切章后需要清空的本章专属规划',
    '- `切章后需重建字段`：切章后需要重建的字段',
    '',
    '## 5. 必须清空的内容（切章后）',
    '- 当前章任务',
    '- 当前章待触发事件',
    '- 当前章镜头规划',
    '- 当前章女主互动事件',
    '- 当前章女主镜头规划',
    '',
    '## 6. 允许保留的内容（跨章）',
    '- 跨章延续事项',
    '- 长期分歧线（仅同人模式）',
    '- 跨章女主关系线',
    '- 长期世界事件',
    '',
    '</剧情规划协议>'
].join('\n');

// Alias for planUpdateReference.ts compatibility
export const 剧情规划变量结构提示词 = 构建原创剧情规划协议();

export const 运行时_剧情规划Schema: 提示词结构 = {
    id: 'runtime_story_plan_schema',
    标题: '剧情规划Schema',
    内容: 构建原创剧情规划协议(),
    类型: '运行时Schema',
    启用: false
};
