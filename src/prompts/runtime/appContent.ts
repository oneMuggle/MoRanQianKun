// App 内容生成 Prompt — 为每个 App 定义 AI 内容生成规则

import type { 游戏设置结构 } from '@/types';

// ============================================================
// 通用 App 内容生成
// ============================================================

export const 短信内容生成提示 = `
## 短信 App 内容生成规则

当需要生成短信内容时（新消息推送、会话初始化）：

### 格式要求
- 短信以会话列表形式出现，每条消息包含：发送者、内容、时间戳
- 发送者使用游戏内已存在的 NPC 姓名或系统通知名
- 内容简短（不超过 50 字），符合日常短信风格

### 内容类型
1. **日常聊天**：熟人之间的问候、闲聊、约饭等
2. **通知推送**：工作提醒、天气预报、账单通知等
3. **紧急消息**：突发事件、重要通知（较少出现）
4. **亲密对话**：随亲密度升级，对话语气更亲密、暧昧

### NSFW 分级控制
- nsfwLevel=0（全年龄）：正常日常对话
- nsfwLevel=1（轻度暗示）：暧昧语气、暗示性对话
- nsfwLevel=2（中度）：亲密接触描写、暧昧邀约
- nsfwLevel=3（重度）：直接描写（需游戏配置允许）

### 生成约束
- 短信内容必须与游戏当前状态一致（时间、地点、关系）
- 不能出现游戏内不存在的 NPC 发信
- 新消息频率：每次 AI 回合最多生成 1~3 条新短信
`.trim();

export const 社交媒体内容生成提示 = `
## 社交媒体 App 内容生成规则

当需要生成社交媒体动态时：

### 格式要求
- 动态包含：发布者、头像、内容文本、点赞数、评论数、发布时间
- 发布者必须是游戏内已存在的 NPC
- 内容长度 20~100 字

### 内容类型
1. **日常分享**：吃饭、天气、心情、自拍
2. **工作相关**：加班吐槽、升职庆祝、工作感悟
3. **社交互动**：聚会照片、朋友合照、节日祝福
4. **暗示动态**：随 NSFW 等级升级，出现擦边暗示内容

### NSFW 分级控制
- nsfwLevel=0：正常生活动态
- nsfwLevel=1：轻微暗示（穿着、自拍角度）
- nsfwLevel=2：亲密合照、暧昧文案
- nsfwLevel=3：直接暗示（需游戏配置允许）

### 生成约束
- 动态内容符合 NPC 性格和身份
- 点赞/评论数为合理范围（0~500）
- 每次 AI 回合最多生成 3~5 条新动态
`.trim();

export const 新闻内容生成提示 = `
## 新闻 App 内容生成规则

当需要生成新闻内容时：

### 格式要求
- 新闻包含：标题、摘要、发布时间、分类
- 标题简洁有力（10~20 字）
- 摘要 50~150 字

### 内容类型
1. **时政新闻**：政策变动、社会热点
2. **娱乐八卦**：明星绯闻、热播剧集
3. **生活资讯**：天气预警、交通管制
4. **游戏内事件**：与当前世界状态相关的新闻

### 生成约束
- 新闻内容符合游戏当前时代背景
- 现代纪元使用现代新闻风格
- 每次 AI 回合最多生成 3~5 条新新闻
`.trim();

export const 天气内容生成提示 = `
## 天气 App 内容生成规则

当需要生成天气数据时：

### 格式要求
- 当前天气：温度、湿度、风力、紫外线指数、天气状况
- 未来 3 天预报：最高/最低温度、天气状况
- 温度范围：根据游戏内季节和城市调整

### 生成约束
- 天气与游戏世界状态一致（环境.天气）
- 现代纪元使用摄氏度
- 天气影响游戏内活动（雨天不适合外出等）
`.trim();

// ============================================================
// 背景匹配 App 内容生成
// ============================================================

export const 工作接单内容生成提示 = `
## 工作接单 App 内容生成规则

当需要生成工作订单/任务时（网约车、外卖配送等）：

### 格式要求
- 订单包含：起点、终点、费用、距离、预计时间、状态
- 起点/终点使用游戏内已有地点名称
- 费用合理（网约车 10~80 元，外卖 5~20 元）

### 内容类型
1. **普通订单**：常规接送/配送
2. **高价值订单**：远途、夜间、恶劣天气（较高报酬）
3. **特殊订单**：VIP 客户、指定服务（可能触发剧情）

### 生成约束
- 订单数量根据时间段调整（早晚高峰多）
- 收入统计与游戏内金钱同步
- 每次 AI 回合最多生成 1~3 个新订单
`.trim();

export const 工作日程内容生成提示 = `
## 工作日程 App 内容生成规则

当需要生成工作日程/任务时：

### 格式要求
- 任务包含：时间、标题、类型、状态
- 时间格式：HH:MM
- 状态：待完成/进行中/已完成/已取消

### 内容类型
1. **日常工作**：会议、打卡、报告
2. **临时任务**：紧急项目、加班
3. **人事相关**：内部消息、通知

### 生成约束
- 任务与角色身份/职业匹配
- 工作时间符合现实（早 9 晚 6）
- 每次 AI 回合最多更新 1~2 个任务状态
`.trim();

export const 预约管理内容生成提示 = `
## 预约管理 App 内容生成规则

当需要生成客户预约时（理发、美容、健身等）：

### 格式要求
- 预约包含：时间、客户名、服务类型、确认状态
- 客户名使用游戏内已有 NPC 或随机生成中文姓名
- 服务类型符合职业特点

### 内容类型
1. **已确认预约**：客户已预约并确认
2. **待确认预约**：新预约等待确认
3. **取消预约**：客户临时取消

### 生成约束
- 预约时间分布合理（不会同一时段多人）
- 客户身份可能触发社交关系
- 每天 3~8 个预约为合理范围
`.trim();

export const 记账本内容生成提示 = `
## 记账本 App 内容生成规则

当需要生成记账记录时：

### 格式要求
- 记录包含：时间、类型、金额、备注
- 类型：收入（绿色）/ 支出（红色）
- 金额合理（便利店日收入 500~3000 元）

### 内容类型
1. **营业收入**：日常经营收入
2. **进货支出**：商品采购成本
3. **日常开支**：水电、租金、杂费

### 生成约束
- 收支与游戏内金钱同步
- 库存变化同步更新
- 每天 5~20 条记录为合理范围
`.trim();

export const 房源管理内容生成提示 = `
## 房源管理 App 内容生成规则

当需要生成房源/客户信息时：

### 格式要求
- 房源包含：标题、面积、价格、区域、标签
- 客户包含：姓名、预算、需求、跟进状态
- 价格合理（一线城市 3000~15000 元/月）

### 内容类型
1. **新房源**：新增待租房源
2. **客户跟进**：新客户咨询、看房预约
3. **成交记录**：已签约/已退租

### 生成约束
- 房源信息符合游戏内城市背景
- 客户可能触发社交关系
- 每周 2~5 套新房源为合理范围
`.trim();

// ============================================================
// 可选 App 内容生成
// ============================================================

export const 论坛内容生成提示 = `
## 论坛 App 内容生成规则

当需要生成论坛帖子时：

### 格式要求
- 帖子包含：作者、标题、标签、回复数、浏览数
- 标题 10~30 字
- 标签：求助/分享/讨论/吐槽/资讯

### 内容类型
1. **求助帖**：问题求助、经验求分享
2. **讨论帖**：热点话题、观点碰撞
3. **分享帖**：个人经历、好物推荐
4. **资讯帖**：新闻搬运、政策解读

### 生成约束
- 帖子内容符合游戏内时代背景
- 回复数/浏览数合理（0~5000）
- 每次 AI 回合最多生成 2~4 条新帖子
`.trim();

export const 购物内容生成提示 = `
## 购物 App 内容生成规则

当需要生成商品/订单时：

### 格式要求
- 商品包含：名称、价格、销量、描述
- 价格合理（日常用品 10~500 元）
- 销量 0~10000+

### 内容类型
1. **推荐商品**：根据身份推荐相关商品
2. **限时优惠**：打折促销活动
3. **订单状态**：待发货/运输中/已签收

### 生成约束
- 商品与游戏内时代匹配
- 购买支出与游戏内金钱同步
- 每次 AI 回合最多更新 1~2 个订单状态
`.trim();

// ============================================================
// NSFW App 内容生成
// ============================================================

export const 约会交友内容生成提示 = `
## 约会交友 App 内容生成规则

当需要生成匹配/聊天内容时：

### 格式要求
- 匹配对象：姓名、年龄、距离、简介、头像
- 年龄范围：18~40 岁
- 距离：0.1~20km
- 简介 10~30 字

### 内容类型
1. **匹配推荐**：AI 驱动的智能推荐
2. **聊天消息**：匹配后的对话
3. **约会邀请**：线下见面邀约

### NSFW 分级控制
- nsfwLevel=1：暧昧聊天、暗示性对话
- nsfwLevel=2：亲密接触描写、约会升级
- nsfwLevel=3：直接描写（需游戏配置允许）

### 生成约束
- 匹配对象可能是游戏内已有 NPC
- 新对象随机生成时使用中文姓名
- 聊天内容符合双方性格
- 约会可能触发游戏内事件
- 每次 AI 回合最多生成 2~4 个新匹配
`.trim();

export const 成人论坛内容生成提示 = `
## 成人论坛 App 内容生成规则

当需要生成成人论坛帖子时：

### 格式要求
- 帖子包含：作者（匿名）、标题、标签、回复数、浏览数
- 作者统一显示为"匿名用户"或随机昵称
- 标题 10~30 字

### 分区
1. **热门**：高回复帖子、热点话题
2. **匿名**：完全匿名发帖
3. **情感**：情感咨询、关系困惑

### NSFW 分级控制
- nsfwLevel=1：暧昧话题、擦边讨论
- nsfwLevel=2：亲密经验交流
- nsfwLevel=3：直接讨论（需游戏配置允许）

### 生成约束
- 内容不泄露游戏内真实身份
- 帖子内容可能与主线剧情产生微妙关联
- 每次 AI 回合最多生成 2~3 条新帖子
`.trim();

export const 私密相册内容生成提示 = `
## 私密相册 App 内容生成规则

当需要生成创作者/订阅内容时：

### 格式要求
- 创作者：名称、粉丝数、图标、锁定状态
- 粉丝数：100~10000
- 图标使用 emoji

### 内容类型
1. **推荐创作者**：AI 推荐的优质创作者
2. **订阅内容**：已订阅创作者的更新
3. **付费解锁**：需要付费的专属内容

### NSFW 分级控制
- nsfwLevel=1：轻度暗示内容
- nsfwLevel=2：中度内容（亲密描写）
- nsfwLevel=3：重度内容（需游戏配置允许）

### 生成约束
- 创作者名称使用中文网名
- 内容描述使用文字而非图片
- 订阅费用合理（月费 20~200 元）
`.trim();

export const 直播内容生成提示 = `
## 直播 App 内容生成规则

当需要生成直播内容时：

### 格式要求
- 直播间：标题、主播、观看人数、标签、图标
- 观看人数：100~50000
- 标签：聊天/音乐/游戏/舞蹈/其他

### 内容类型
1. **推荐直播**：AI 推荐的热门直播
2. **分类浏览**：按标签分类
3. **弹幕互动**：直播间弹幕消息

### NSFW 分级控制
- 正常模式：全年龄内容
- nsfwLevel=1：擦边内容暗示
- nsfwLevel=2：分区切换后可见
- nsfwLevel=3：NSFW 分区（需游戏配置允许）

### 生成约束
- 直播间标题有趣、吸引点击
- 弹幕内容符合直播间主题
- 每次 AI 回合最多生成 3~5 个新直播间
`.trim();

// ============================================================
// 综合协议
// ============================================================

/**
 * 获取手机 App 内容生成协议
 * 返回所有 App 内容生成规则的合并文本
 */
export const 获取手机App内容生成协议 = (config?: Partial<游戏设置结构> | null): string => {
  const nsfwEnabled = config && '启用NSFW模式' in config ? config.启用NSFW模式 : false;

  const parts = [
    '## 手机 App 内容生成协议',
    '',
    '以下规则定义各类型 App 的 AI 内容生成标准。',
    '',
    短信内容生成提示,
    '',
    社交媒体内容生成提示,
    '',
    新闻内容生成提示,
    '',
    天气内容生成提示,
    '',
    工作接单内容生成提示,
    '',
    工作日程内容生成提示,
    '',
    预约管理内容生成提示,
    '',
    记账本内容生成提示,
    '',
    房源管理内容生成提示,
    '',
    论坛内容生成提示,
    '',
    购物内容生成提示,
  ];

  if (nsfwEnabled) {
    parts.push(
      '',
      约会交友内容生成提示,
      '',
      成人论坛内容生成提示,
      '',
      私密相册内容生成提示,
      '',
      直播内容生成提示,
    );
  }

  return parts.join('\n');
};
